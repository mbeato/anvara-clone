---
phase: 07-polish-and-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_components/landing/hero-section.tsx
  - app/_components/landing/final-cta.tsx
  - app/_components/landing/anvara-intelligence.tsx
  - app/_components/landing/landing-navbar.tsx
  - app/_components/landing/performance-reporting.tsx
  - app/_components/landing/for-brands-rightsholders.tsx
  - app/_components/landing/access-market.tsx
  - app/_components/landing/what-is-anvara.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page renders at 375px with no horizontal scroll"
    - "Hero typewriter prompt does not overflow viewport at 375px"
    - "Hero card carousel shows cards at a readable size on mobile"
    - "FinalCTA vertical scroll columns stack properly on mobile"
    - "All landing sections are readable and intentionally laid out at 375px"
  artifacts:
    - path: "app/_components/landing/hero-section.tsx"
      provides: "Mobile-safe typewriter and card carousel"
      contains: "whitespace-normal"
    - path: "app/_components/landing/final-cta.tsx"
      provides: "Stacked CTA on mobile"
      contains: "lg:grid-cols-2"
  key_links:
    - from: "app/page.tsx"
      to: "all landing components"
      via: "Server Component import"
      pattern: "import.*landing"
---

<objective>
Fix all landing page components for mobile (375px) — the highest-priority page per user decision since it's the public-facing page founders will share.

Purpose: The landing page is the first thing founders see and share. It must feel intentionally designed at 375px, not just "not broken."
Output: All 12+ landing sections render beautifully at 375px with no horizontal scroll.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-deploy/07-CONTEXT.md
@.planning/phases/07-polish-and-deploy/07-RESEARCH.md

@app/page.tsx
@app/_components/landing/hero-section.tsx
@app/_components/landing/final-cta.tsx
@app/_components/landing/anvara-intelligence.tsx
@app/_components/landing/performance-reporting.tsx
@app/_components/landing/for-brands-rightsholders.tsx
@app/_components/landing/landing-navbar.tsx
@app/_components/landing/access-market.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hero section mobile layout</name>
  <files>app/_components/landing/hero-section.tsx</files>
  <action>
Fix three mobile issues in the hero section:

1. **Typewriter prompt bar overflow:** The `whitespace-nowrap` on line 190 prevents text wrapping at 375px. Change to `whitespace-normal sm:whitespace-nowrap` so the prompt text wraps on mobile but stays single-line on desktop. Also reduce horizontal padding on the prompt bar from `px-5` to `px-3 sm:px-5` for tighter fit.

2. **Hero card carousel sizing:** The three `w-[320px]` LandingPropertyCards in a `flex gap-4` row overflow at 375px. On mobile, show only one card centered (hide the other two) OR reduce card width to fit. Best approach: add `hidden sm:block` to the 2nd and 3rd `m.div` wrappers so only one card shows on mobile, keeping it clean and focused. Alternatively, make the card container scrollable: `overflow-x-auto` on the flex wrapper.

3. **Hero heading font size:** `text-4xl` at 375px may be too large. Consider `text-3xl sm:text-4xl` to give breathing room.

4. **Brand logos marquee:** The `w-[60%]` on the marquee wrapper may be too narrow on mobile. Change to `w-full sm:w-[60%]` so logos fill the width.

5. **CTA button:** Ensure the "Try Anvara Free" button has adequate padding at 375px — current `px-7 py-3.5` is fine.

Keep all desktop layouts exactly the same — only add mobile-first base classes.
  </action>
  <verify>Open the app at 375px in browser DevTools. The hero section should display: heading readable, typewriter prompt wraps without overflow, one property card centered, brand logos visible, no horizontal scroll.</verify>
  <done>Hero section renders cleanly at 375px with no overflow, readable text, and intentional single-card display.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining landing sections for mobile</name>
  <files>
    app/_components/landing/final-cta.tsx
    app/_components/landing/anvara-intelligence.tsx
    app/_components/landing/performance-reporting.tsx
    app/_components/landing/for-brands-rightsholders.tsx
    app/_components/landing/access-market.tsx
    app/_components/landing/what-is-anvara.tsx
  </files>
  <action>
Audit and fix each landing section at 375px:

**FinalCTA (final-cta.tsx):**
- The `grid grid-cols-1 lg:grid-cols-2` is already correct for stacking on mobile.
- The vertical scrolling card columns `h-[480px]` may be excessive on mobile. Add `h-[320px] lg:h-[480px]` to reduce height on mobile.
- The `px-8` padding may be too wide at 375px. Change to `px-4 lg:px-8`.
- The heading `text-4xl lg:text-5xl` is fine.

**AnvaraIntelligence (anvara-intelligence.tsx):**
- Already uses `flex-col lg:flex-row`. Check inner panel widths.
- If mockup panels use fixed widths (`max-w-[260px]` etc.), ensure they don't overflow. They should be fine since they're inside flex-col on mobile.
- Reduce any `px-8` to `px-4 lg:px-8` for tighter mobile margins.

**PerformanceReporting (performance-reporting.tsx):**
- Check for any fixed widths or multi-column layouts that break at 375px.
- Ensure metric cards stack vertically on mobile.
- Reduce horizontal padding if needed.

**ForBrandsRightsholders (for-brands-rightsholders.tsx):**
- Logo grids should wrap at 375px. Ensure `flex-wrap` is present.
- Reduce any oversized heading text for mobile.
- Check padding/margins.

**AccessMarket (access-market.tsx):**
- Property card grid should already be responsive.
- Ensure heading and description have proper mobile padding.
- Reduce `px-8` to `px-4 lg:px-8` if needed.

**WhatIsAnvara (what-is-anvara.tsx):**
- The typewriter `<span>` on line ~255 has `whitespace-nowrap` which will overflow at 375px. Change to `whitespace-normal sm:whitespace-nowrap` so text wraps on mobile.
- Check heading and body text sizes for mobile readability.
- Ensure horizontal padding follows the `px-4 lg:px-8` pattern.
- Verify no fixed-width elements exceed 343px.

Pattern: For all sections, ensure `px-4` base with `lg:px-8` for desktop, and that no element has a fixed width wider than 343px (375 - 16px padding each side).
  </action>
  <verify>Scroll through the entire landing page at 375px in DevTools. All sections should be readable, properly stacked, no horizontal scroll on any section.</verify>
  <done>All landing page sections render cleanly at 375px — padding is tight, grids stack, no overflow, text is readable.</done>
</task>

</tasks>

<verification>
1. Load the landing page (`/`) at 375px viewport width in Chrome DevTools
2. Scroll through all 12 sections — no horizontal scrollbar should appear
3. Hero typewriter text wraps or fits within viewport
4. Hero cards display cleanly (single card or scrollable)
5. FinalCTA cards are visible and not cut off
6. All text is readable at mobile font sizes
7. Load at 1280px and verify no desktop regressions — all existing layouts unchanged
</verification>

<success_criteria>
- Landing page at 375px: zero horizontal scroll, all sections readable, intentional mobile layout
- Landing page at 1280px: no regressions from current desktop layout
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-deploy/07-01-SUMMARY.md`
</output>
