---
phase: 07-polish-and-deploy
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - package.json
autonomous: true

must_haves:
  truths:
    - "`npm run build` completes with zero errors and zero warnings"
    - "package.json has postinstall script for prisma generate"
    - "noindex meta tag is present in production build HTML"
  artifacts:
    - path: "package.json"
      provides: "Vercel-ready scripts with postinstall"
      contains: "postinstall"
  key_links:
    - from: "package.json"
      to: "prisma/schema.prisma"
      via: "postinstall prisma generate"
      pattern: "postinstall.*prisma generate"
---

<objective>
Run `npm run build` to verify zero errors/warnings, add `postinstall` script for Vercel deployment, and audit for any console errors.

Purpose: Ensure the build is clean before deploying. The postinstall script is critical — without it, Vercel's build will fail because Prisma's generated client is not committed to git.
Output: Clean build output, production-ready package.json.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-deploy/07-CONTEXT.md
@.planning/phases/07-polish-and-deploy/07-RESEARCH.md

@package.json
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postinstall script and verify clean build</name>
  <files>package.json</files>
  <action>
1. **Add postinstall script to package.json:**
   In the `"scripts"` section, add `"postinstall": "prisma generate"` BEFORE the `"dev"` entry. The scripts section should be:
   ```json
   "scripts": {
     "postinstall": "prisma generate",
     "dev": "next dev",
     "build": "next build",
     "start": "next start",
     "lint": "eslint"
   }
   ```

2. **Run `npm run build`** and capture full output. Verify:
   - Exit code is 0
   - No TypeScript errors
   - No build warnings
   - All routes compile successfully

3. **If build fails or has warnings:** Fix the issues. Common causes after mobile responsive changes:
   - Missing imports (e.g., `useIsMobile`, `ChevronLeft`)
   - TypeScript errors from new state variables
   - Unused imports after refactoring

4. **Verify noindex:** Check that `app/layout.tsx` still has the `robots: { index: false, follow: false }` metadata export. This generates the noindex meta tag. Already confirmed present — just double-check it wasn't accidentally removed.

5. **Console error audit:** Start the dev server (`npm run dev`) and visit each major page in order. Check DevTools console for errors on each:
   - `/` (landing page)
   - `/listings` (browse page)
   - `/listings/[any-slug]` (property detail)
   - `/messages` (messaging)
   - `/dashboard` (analytics)
   - `/campaigns` (campaigns table)
   - `/deals` (deals pipeline)
   - `/ai` (AI chat)
   - `/settings` (settings page)

   Document any console errors found. Fix production-visible errors. Ignore dev-only Next.js/React warnings per user decision.
  </action>
  <verify>
Run `npm run build` — exit code 0, zero errors, zero warnings in output.
Check `app/layout.tsx` contains `robots: { index: false, follow: false }`.
  </verify>
  <done>`npm run build` passes cleanly. `postinstall` script present. noindex confirmed. Console errors audited and fixed.</done>
</task>

</tasks>

<verification>
1. `npm run build` exit code is 0
2. Build output contains no "error" or "warning" lines
3. `package.json` has `"postinstall": "prisma generate"` in scripts
4. `app/layout.tsx` metadata has `robots: { index: false, follow: false }`
5. Console errors audited on all major pages
</verification>

<success_criteria>
- Build is clean — zero errors, zero warnings
- postinstall script present for Vercel
- noindex meta tag confirmed
- All production-visible console errors fixed
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-deploy/07-03-SUMMARY.md`
</output>
