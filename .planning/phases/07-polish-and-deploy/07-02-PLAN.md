---
phase: 07-polish-and-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/listings/[slug]/page.tsx
  - app/(app)/listings/[slug]/loading.tsx
  - components/messaging/messaging-client.tsx
  - components/header-bar.tsx
  - app/(app)/dashboard/_components/recent-activity-table.tsx
autonomous: true

must_haves:
  truths:
    - "Property detail page renders in single-column layout at 375px with sidebar below content"
    - "Messaging client shows either thread list OR conversation on mobile, not both"
    - "Header bar does not overflow or look cramped at 375px"
    - "Dashboard recent activity table scrolls horizontally on mobile without breaking page layout"
    - "Property detail loading skeleton matches responsive grid layout"
  artifacts:
    - path: "app/(app)/listings/[slug]/page.tsx"
      provides: "Responsive property detail grid"
      contains: "lg:grid-cols-5"
    - path: "components/messaging/messaging-client.tsx"
      provides: "Mobile panel switching for messaging"
      contains: "useIsMobile"
    - path: "components/header-bar.tsx"
      provides: "Mobile-safe header bar"
      contains: "hidden sm:flex"
  key_links:
    - from: "components/messaging/messaging-client.tsx"
      to: "hooks/use-mobile"
      via: "useIsMobile import"
      pattern: "import.*useIsMobile"
---

<objective>
Fix all app-shell pages for mobile (375px) — property detail, messaging, header bar, and dashboard table.

Purpose: Every page inside the app shell should feel polished at 375px — intentionally designed, not just functional. The sidebar already handles mobile (Sheet overlay), so this plan focuses on page content.
Output: All app pages render cleanly at 375px with proper stacking, panel switching, and no overflow.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-deploy/07-CONTEXT.md
@.planning/phases/07-polish-and-deploy/07-RESEARCH.md

@app/(app)/listings/[slug]/page.tsx
@app/(app)/listings/[slug]/loading.tsx
@components/messaging/messaging-client.tsx
@components/header-bar.tsx
@app/(app)/dashboard/_components/recent-activity-table.tsx
@hooks/use-mobile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix property detail page and loading skeleton for mobile</name>
  <files>
    app/(app)/listings/[slug]/page.tsx
    app/(app)/listings/[slug]/loading.tsx
  </files>
  <action>
**Property detail page (`page.tsx`):**

Line 25 currently has `grid grid-cols-5 gap-8`. Fix to responsive:

```
<div className="grid grid-cols-1 lg:grid-cols-5 gap-6 lg:gap-8 items-start">
```

Left column (line 27): Change `col-span-3` to `lg:col-span-3` (no col-span on mobile — full width by default in grid-cols-1).

Right column (line 57): Change `col-span-2 sticky top-20 mt-20` to `lg:col-span-2 lg:sticky lg:top-20 mt-8 lg:mt-20`. Remove sticky behavior on mobile (causes issues in single-column layout). Reduce `mt-20` to `mt-8` on mobile to avoid excessive gap between content and sidebar.

**Loading skeleton (`loading.tsx`):**

Apply identical grid fix:
- Line 6: `grid grid-cols-5 gap-8` → `grid grid-cols-1 lg:grid-cols-5 gap-6 lg:gap-8`
- Line 8: `col-span-3` → `lg:col-span-3`
- Line 34: `col-span-2` → `lg:col-span-2`

Keep desktop layout (1280px) identical — only add mobile-first base classes.
  </action>
  <verify>Navigate to any property detail page (e.g., `/listings/coachella-music-festival`) at 375px. Content should be single column with offer sidebar below. At 1280px, should be 60/40 two-column with sticky sidebar.</verify>
  <done>Property detail page and loading skeleton are single-column on mobile and two-column on desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Fix messaging client and header bar for mobile</name>
  <files>
    components/messaging/messaging-client.tsx
    components/header-bar.tsx
    app/(app)/dashboard/_components/recent-activity-table.tsx
  </files>
  <action>
**Messaging client (`messaging-client.tsx`):**

The fixed `w-80` thread list on line 76 leaves only 55px for conversation at 375px. Implement mobile panel switching:

1. Import `useIsMobile` from `@/hooks/use-mobile`
2. Add state: `const [mobileView, setMobileView] = useState<"threads" | "conversation">("threads")`
3. In `selectThread` function, add: `if (isMobile) setMobileView("conversation")`
4. Change the thread list div (line 76) to conditionally show/hide:
   - On desktop: always visible with `w-80`
   - On mobile: visible only when `mobileView === "threads"`, full width (`w-full`)
   - Use: `className={cn(isMobile && mobileView !== "threads" ? "hidden" : "flex", isMobile ? "w-full" : "w-80", "border-r flex-col overflow-hidden shrink-0")}`
5. Change the conversation div (line 85) to conditionally show/hide:
   - On desktop: always visible
   - On mobile: visible only when `mobileView === "conversation"`
6. Add a back button at the top of the conversation view on mobile:
   ```tsx
   {isMobile && mobileView === "conversation" && (
     <button
       onClick={() => setMobileView("threads")}
       className="flex items-center gap-1 px-4 py-2 text-sm text-muted-foreground hover:text-foreground border-b"
     >
       <ChevronLeft className="h-4 w-4" /> Back to threads
     </button>
   )}
   ```
7. Import `ChevronLeft` from `lucide-react`
8. Also handle the compose view: when `composing` is true on mobile, show compose view full-width (hide thread list).
9. When a new thread is started successfully and navigating to it, also set `mobileView("conversation")`.

**Header bar (`header-bar.tsx`):**

The search bar (line 89) crowds the header at 375px. Hide it on mobile:

Change line 89's div from:
```
<div className="flex max-w-xs flex-1 items-center gap-2">
```
to:
```
<div className="hidden sm:flex max-w-xs flex-1 items-center gap-2">
```

This hides search at <640px. The breadcrumb, sidebar trigger, notifications, and avatar remain visible.

Also: the `<Separator>` and "Demo Advertiser" text are already `hidden md:block` — good. But check if the notifications + mode toggle + avatar row is too crowded at 375px. If so, hide mode toggle on mobile: add `hidden sm:flex` wrapper around `<ModeToggle />`.

**Dashboard table (`recent-activity-table.tsx`):**

Wrap the `<table>` in `<div className="overflow-x-auto">` to enable horizontal scrolling on mobile instead of breaking the page layout. The table currently has no wrapper.
  </action>
  <verify>
1. Navigate to `/messages` at 375px — thread list should fill the width. Tap a thread — conversation should fill the width with a "Back to threads" button visible.
2. Check header bar at 375px — no search bar, icons fit, no overflow.
3. Navigate to `/dashboard` at 375px — Recent Activity table should scroll horizontally if needed, no page overflow.
  </verify>
  <done>Messaging shows single-panel mobile view with back navigation, header bar is clean at 375px, dashboard table scrolls horizontally.</done>
</task>

</tasks>

<verification>
1. Property detail at 375px: single column, sidebar below content, no sticky issues
2. Property detail at 1280px: unchanged two-column with sticky sidebar
3. Messaging at 375px: thread list fills screen, tapping thread switches to conversation, back button returns to list
4. Messaging at 1280px: unchanged two-panel layout
5. Header bar at 375px: sidebar trigger, breadcrumb, and action icons visible, no search bar, no overflow
6. Dashboard at 375px: table scrolls horizontally if wider than viewport
7. All pages: no horizontal scroll on the body
</verification>

<success_criteria>
- All app pages render cleanly at 375px with intentional mobile layouts
- Messaging has functional panel switching (thread list / conversation) on mobile
- No regressions at 1280px desktop width
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-deploy/07-02-SUMMARY.md`
</output>
