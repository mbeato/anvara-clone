---
phase: 06.2-public-listing-page
plan: 02
subsystem: ui
tags: [nextjs, landing-page, public-listing, slug, linking]

# Dependency graph
requires:
  - phase: 06.2-01
    provides: Public /p/[slug] page that landing cards now link to
provides:
  - All landing page property cards link to /p/{slug} (public listing page)
  - slug field flows from Prisma through page.tsx to all landing section components
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Slug-based public URLs: landing cards use /p/{slug} not /listings/{id}"
    - "Single source of truth for href: LandingPropertyCard owns the href, consumers pass slug"

key-files:
  created: []
  modified:
    - app/page.tsx
    - app/_components/landing/landing-property-card.tsx
    - app/_components/landing/access-market.tsx
    - app/_components/landing/hero-section.tsx
    - app/_components/landing/final-cta.tsx

key-decisions:
  - "All landing card hrefs centralized in LandingPropertyCard — access-market and final-cta don't set their own hrefs"
  - "HeroSection uses ...prop spread in cardGroups memo — slug carries through automatically without enumerating fields"

patterns-established:
  - "Public browsing flow: landing page -> /p/{slug} (public, no auth required)"
  - "Private browsing flow: /listings -> /listings/{slug} (requires auth)"

# Metrics
duration: 2min
completed: 2026-02-23
---

# Phase 06.2 Plan 02: Landing Page Slug Links Summary

**Landing page property cards updated from /listings/{id} to /p/{slug}, completing the public browsing flow from landing page to public listing page**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-23T01:11:04Z
- **Completed:** 2026-02-23T01:12:45Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- All landing page property cards (hero carousel, AccessMarket scroll, FinalCTA columns) now navigate to `/p/{slug}`
- `slug` field added to the shared property mapping in `app/page.tsx`, flowing to all consumers
- TypeScript types updated across all 4 landing section components — build passes cleanly
- Zero remaining `/listings/${property.id}` references in landing components

## Task Commits

Each task was committed atomically:

1. **Task 1: Add slug to property mapping and update LandingPropertyCard** - `7885151` (feat)
2. **Task 2: Update AccessMarket, HeroSection, and FinalCTA Property types** - `5614c76` (feat)

**Plan metadata:** TBD (docs: complete plan)

## Files Created/Modified
- `app/page.tsx` - Added `slug: p.slug` to the rawProperties map so slug flows to all landing sections
- `app/_components/landing/landing-property-card.tsx` - Added `slug: string` to Property type; changed href from `/listings/${property.id}` to `/p/${property.slug}`
- `app/_components/landing/access-market.tsx` - Added `slug: string` to AccessMarketProps array type; changed PropertyRow href from `/listings/${property.id}` to `/p/${property.slug}`
- `app/_components/landing/hero-section.tsx` - Added `slug: string` to Property type (enables slug passthrough via `...prop` spread in cardGroups memo)
- `app/_components/landing/final-cta.tsx` - Added `slug: string` to Property type (enables slug passthrough to LandingPropertyCard)

## Decisions Made
- Hrefs are owned by the leaf component (`LandingPropertyCard` for hero/final-cta, `PropertyRow` in access-market) — parent components only need to pass slug in their type
- `HeroSection` uses `...prop` spread in cardGroups memo — slug propagates automatically without field enumeration (plan cautioned against rewriting to explicit fields)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 6.2 complete — public listing page foundation (06.2-01) and landing page linking (06.2-02) both done
- Ready for Phase 6.3: AI Chat with Listing Recommendations
- The private `/listings/[slug]` route is unchanged and continues to work for authenticated users

---
*Phase: 06.2-public-listing-page*
*Completed: 2026-02-23*

## Self-Check: PASSED
