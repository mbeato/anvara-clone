---
phase: 03-property-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/properties/[slug]/page.tsx
  - app/(app)/properties/[slug]/loading.tsx
  - components/property-detail/property-hero.tsx
  - components/property-detail/property-meta.tsx
  - components/property-detail/property-about.tsx
  - components/ui/carousel.tsx
  - components/ui/badge.tsx
autonomous: true

must_haves:
  truths:
    - "Detail page loads for any seeded property slug and displays hero image, title, category, date, and location"
    - "Image carousel renders with navigation arrows and dot indicators"
    - "About this Listing section truncates to 3-4 lines with a working Read more/Read less toggle"
    - "Page uses 60/40 grid layout with items-start for sticky sidebar compatibility"
  artifacts:
    - path: "app/(app)/properties/[slug]/page.tsx"
      provides: "Async server component with params: Promise, notFound(), 60/40 grid layout"
      contains: "await params"
    - path: "components/property-detail/property-hero.tsx"
      provides: "Image carousel using ShadCN Carousel with dots and arrows"
      exports: ["PropertyHero"]
    - path: "components/property-detail/property-meta.tsx"
      provides: "Property name, category badge, date, location row"
      exports: ["PropertyMeta"]
    - path: "components/property-detail/property-about.tsx"
      provides: "Truncated description with Read more toggle"
      exports: ["PropertyAbout"]
  key_links:
    - from: "app/(app)/properties/[slug]/page.tsx"
      to: "lib/data/index.ts"
      via: "getPropertyBySlug import"
      pattern: "getPropertyBySlug"
    - from: "app/(app)/properties/[slug]/page.tsx"
      to: "components/property-detail/property-hero.tsx"
      via: "component import and render"
      pattern: "PropertyHero"
---

<objective>
Build the property detail page route and its top-half components: the hero image carousel, property metadata row, and "About this Listing" section with truncation.

Purpose: Establishes the page route (Next.js 16 async params pattern), the 60/40 grid layout skeleton for the sticky sidebar, and the first three visual sections that an advertiser sees when landing on a property.

Output: A navigable detail page at `/properties/[slug]` that renders hero carousel, metadata, and about section. Sidebar column is a placeholder div ready for Plan 03-02.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-property-detail/03-RESEARCH.md
@prisma/schema.prisma
@lib/data/index.ts
@app/(app)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ShadCN components and create property detail page route</name>
  <files>
    app/(app)/properties/[slug]/page.tsx
    app/(app)/properties/[slug]/loading.tsx
    components/ui/carousel.tsx
    components/ui/badge.tsx
  </files>
  <action>
    1. Install required ShadCN components:
       ```bash
       npx shadcn@latest add carousel badge
       ```
       This installs Embla Carousel (for carousel) and the Badge component (for category chips).

    2. Create `app/(app)/properties/[slug]/page.tsx` as an async Server Component:
       - Import `getPropertyBySlug` from `@/lib/data`
       - Import `notFound` from `next/navigation`
       - Type params as `Promise<{ slug: string }>` (Next.js 16 mandatory async params)
       - `const { slug } = await params` — MUST await before accessing slug
       - Fetch property with `getPropertyBySlug(slug)`, call `notFound()` if null
       - Add `generateStaticParams` that returns `[]` (safe fallback for build without DB)
       - Render the 60/40 grid layout:
         ```
         <div className="max-w-6xl mx-auto px-4 py-6">
           <div className="grid grid-cols-5 gap-8 items-start">
             <div className="col-span-3">
               <PropertyHero images={[property.imageUrl]} />
               <div className="mt-6 space-y-8">
                 <PropertyMeta property={property} />
                 <PropertyAbout description={property.description} />
                 {/* Tiers, demographics, categories, formats — added in Plans 03-02 and 03-03 */}
               </div>
             </div>
             <div className="col-span-2 sticky top-20">
               {/* OfferSidebar — added in Plan 03-02 */}
               <div className="rounded-lg border p-6 text-sm text-muted-foreground">Offer sidebar placeholder</div>
             </div>
           </div>
         </div>
         ```
       - CRITICAL: `items-start` on the grid container — required for sticky sidebar to work (without it, grid stretches both columns to equal height, defeating position:sticky)

    3. Create `app/(app)/properties/[slug]/loading.tsx` — a simple loading skeleton:
       - Use ShadCN Skeleton components for the hero area and sidebar placeholder
       - Mirror the 60/40 grid layout with skeleton blocks
  </action>
  <verify>
    - `npm run build` completes without TypeScript errors
    - Navigate to `http://localhost:3000/properties/lafc` — page loads with the grid layout visible
    - Navigate to `http://localhost:3000/properties/nonexistent` — 404 page renders
  </verify>
  <done>Page route exists with async params, 60/40 grid layout renders, notFound works for invalid slugs, loading skeleton shows during data fetch</done>
</task>

<task type="auto">
  <name>Task 2: Build PropertyHero carousel, PropertyMeta, and PropertyAbout components</name>
  <files>
    components/property-detail/property-hero.tsx
    components/property-detail/property-meta.tsx
    components/property-detail/property-about.tsx
  </files>
  <action>
    1. Create `components/property-detail/property-hero.tsx` — Client Component ("use client"):
       - Accept `images: string[]` prop (currently single image, future-proofed for array)
       - Use ShadCN Carousel with `setApi` to track current slide for dot indicators
       - Enable `loop: true` in carousel opts
       - Each CarouselItem: `<div className="relative aspect-[4/3] overflow-hidden rounded-xl">` with `<Image src={src} alt="" fill className="object-contain" />` from `next/image`
         - Use `object-contain` (not object-cover) per user decision: "Images must be properly contained — no cropping tops/bottoms"
         - Add a `bg-muted` background on the container so letterboxed areas aren't blank
       - Render `<CarouselPrevious>` and `<CarouselNext>` arrow buttons positioned inside the carousel
       - Below carousel: dot indicators (small buttons), active dot wider with `bg-primary`, inactive dots narrow with `bg-muted-foreground/40`
       - Import `cn` from `@/lib/utils` for conditional classes

    2. Create `components/property-detail/property-meta.tsx` — Server Component (no "use client"):
       - Accept property object (use the Prisma return type inline or `{ name: string; category: string; location: string; availability: string; tags: string[] }`)
       - Render:
         - Property name as `<h1>` with `text-2xl font-bold`
         - Category as a `<Badge>` component (e.g., `variant="secondary"` — capitalized category name)
         - Location with a MapPin icon from Lucide
         - Availability string with a Calendar icon from Lucide
       - Layout: name on top, then a flex row of badge + location + availability below

    3. Create `components/property-detail/property-about.tsx` — Client Component ("use client"):
       - Accept `description: string` prop
       - State: `expanded` boolean, default false
       - Render section heading "About this Listing" with `text-lg font-semibold`
       - Paragraph with `className={cn("text-muted-foreground leading-relaxed", !expanded && "line-clamp-4")}`
       - "Read more" / "Read less" button below: `text-sm text-primary hover:underline cursor-pointer`
       - Section is positioned ABOVE tiers per user decision
  </action>
  <verify>
    - Navigate to `/properties/lafc` — hero image renders in a carousel with arrows and dots visible
    - Property name "LAFC (Los Angeles FC)" displays as heading
    - Category badge shows "sports"
    - Description truncates to ~4 lines; clicking "Read more" expands; clicking "Read less" collapses
    - Image does NOT crop — `object-contain` ensures full image visible within aspect-[4/3] container
  </verify>
  <done>PropertyHero shows image carousel with dots/arrows, PropertyMeta shows name+badge+location+availability, PropertyAbout truncates with working Read more/less toggle</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `/properties/lafc` renders hero image, name, badge, location, availability, and truncated about section
3. `/properties/ny-jets` renders a different property correctly (proving dynamic route works)
4. `/properties/nonexistent-slug` shows 404
5. The carousel dot indicators and arrows are visible and functional
6. The 60/40 grid is visible with sidebar placeholder on the right
7. Loading skeleton appears briefly when navigating to the page
</verification>

<success_criteria>
- Property detail page route at `app/(app)/properties/[slug]/page.tsx` uses Next.js 16 async params pattern
- Hero carousel renders with ShadCN Carousel (Embla), dot indicators, and navigation arrows
- PropertyMeta displays property name, category badge, location, and availability
- PropertyAbout truncates long descriptions with a Read more/Read less toggle
- 60/40 grid layout with `items-start` is ready for sticky sidebar (placeholder in right column)
- Loading skeleton exists for the route
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-detail/03-01-SUMMARY.md`
</output>
