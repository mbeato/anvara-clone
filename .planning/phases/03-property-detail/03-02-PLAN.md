---
phase: 03-property-detail
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/property-detail/property-tiers.tsx
  - components/property-detail/offer-sidebar.tsx
  - components/property-detail/make-offer-dialog.tsx
  - components/property-detail/build-offer-form.tsx
  - components/ui/dialog.tsx
  - app/(app)/properties/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Pricing tiers display in vertical layout sorted cheapest-to-most-expensive with formatted dollar amounts (never $0)"
    - "Clicking a tier row expands its inclusion bullet list with brand-colored border accent"
    - "Sticky right sidebar shows minimum spend, Make an Offer (green), Message, Schedule Call buttons, and trust badges"
    - "Make an Offer button opens a modal dialog form with amount and terms fields"
    - "Build your own offer section renders below tiers with budget, deliverables checkboxes, and notes fields"
  artifacts:
    - path: "components/property-detail/property-tiers.tsx"
      provides: "Vertical click-to-expand tier list with price formatting"
      exports: ["PropertyTiers"]
    - path: "components/property-detail/offer-sidebar.tsx"
      provides: "Sticky sidebar with CTAs, trust badges, minimum spend"
      exports: ["OfferSidebar"]
    - path: "components/property-detail/make-offer-dialog.tsx"
      provides: "Modal form for offer submission"
      exports: ["MakeOfferDialog"]
    - path: "components/property-detail/build-offer-form.tsx"
      provides: "Structured form for custom offer below tiers"
      exports: ["BuildOfferForm"]
  key_links:
    - from: "app/(app)/properties/[slug]/page.tsx"
      to: "components/property-detail/offer-sidebar.tsx"
      via: "replaces sidebar placeholder"
      pattern: "OfferSidebar"
    - from: "components/property-detail/offer-sidebar.tsx"
      to: "components/property-detail/make-offer-dialog.tsx"
      via: "renders MakeOfferDialog as primary CTA"
      pattern: "MakeOfferDialog"
    - from: "app/(app)/properties/[slug]/page.tsx"
      to: "components/property-detail/property-tiers.tsx"
      via: "renders in left column content area"
      pattern: "PropertyTiers"
---

<objective>
Build the pricing tier section and the sticky offer sidebar with all CTAs, trust badges, and the Make an Offer modal dialog.

Purpose: This is the monetization heart of the detail page — an advertiser evaluates tier pricing, sees trust signals, and can initiate a deal. The sidebar sticks as the user scrolls, keeping the CTA always accessible (like Airbnb's booking widget).

Output: Fully functional tier pricing section with click-to-expand inclusions, sticky sidebar with all buttons and badges, and a Make an Offer modal form.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-property-detail/03-RESEARCH.md
@.planning/phases/03-property-detail/03-01-SUMMARY.md
@prisma/schema.prisma
@lib/data/index.ts
@app/(app)/properties/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ShadCN Dialog and build tier pricing + build-your-own-offer</name>
  <files>
    components/ui/dialog.tsx
    components/property-detail/property-tiers.tsx
    components/property-detail/build-offer-form.tsx
  </files>
  <action>
    1. Install ShadCN Dialog:
       ```bash
       npx shadcn@latest add dialog
       ```

    2. Create `components/property-detail/property-tiers.tsx` — Client Component ("use client"):
       - Accept `packages` prop (array of Package objects from Prisma — `{ id: string; name: string; priceUsd: number; inclusions: string[]; maxSponsors: number }`)
       - State: `selectedId: string | null` — tracks which tier is expanded
       - Sort packages ascending by `priceUsd` (cheapest first at top, most expensive at bottom — per user decision)
       - Render section heading: "Sponsorship Tiers" with `text-lg font-semibold`
       - Each tier as a clickable card:
         - Border: when selected, `border-primary bg-primary/5` (brand-colored accent); when not, `border-border hover:border-muted-foreground/40`
         - Top row: tier name (left, font-medium) and price (right, font-semibold tabular-nums)
         - Price formatted via `Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 })` — produces "$15,000" format
         - NEVER show $0 — if a package has priceUsd of 0, display "Contact for pricing" instead
         - When selected: expand to show inclusions as a `<ul>` with check marks (use Lucide `Check` icon in `text-primary`)
         - `maxSponsors` shown as muted text below inclusions: "{maxSponsors} sponsor slots available"
         - Clicking again collapses (toggle behavior)
       - Add transition-all on the border for smooth visual feedback

    3. Create `components/property-detail/build-offer-form.tsx` — Client Component ("use client"):
       - Section heading: "Build Your Own Offer"
       - Structured form (NOT a submit form — visual only for Phase 3; actual submission in Phase 5):
         - Budget field: `<Input type="number" placeholder="Your budget (USD)" />`
         - Deliverables: checkboxes for common items derived from the property's package inclusions (or static list: "Logo placement", "Social media posts", "VIP access", "On-site activation", "Digital content")
         - Notes: `<textarea>` with placeholder "Describe what you're looking for..."
       - Muted helper text: "Submit via Make an Offer above"
       - This section renders BELOW the tiers in the left column per user decision: "Build your own offer below tiers"
  </action>
  <verify>
    - Navigate to `/properties/lafc` — tiers section shows 3 tiers (Community Partner $25,000, Gold Partner $55,000, Founding Partner $125,000) sorted ascending
    - Click a tier — its inclusions expand with check marks visible
    - Click again — inclusions collapse
    - Selected tier has visibly different border color (brand primary)
    - No tier shows "$0"
    - Build your own offer form renders below tiers with budget input, checkboxes, and notes textarea
  </verify>
  <done>Vertical tier list renders sorted by price with click-to-expand inclusions, brand accent on selected tier, $0 handled, and build-your-own-offer form below</done>
</task>

<task type="auto">
  <name>Task 2: Build offer sidebar with CTAs, trust badges, and Make an Offer dialog; wire into page</name>
  <files>
    components/property-detail/offer-sidebar.tsx
    components/property-detail/make-offer-dialog.tsx
    app/(app)/properties/[slug]/page.tsx
  </files>
  <action>
    1. Create `components/property-detail/offer-sidebar.tsx` — Client Component ("use client"):
       - Accept property data props: `{ propertyId: string; propertyName: string; priceFrom: number; packageCount: number }`
       - Layout: card with `rounded-xl border bg-card p-6 space-y-4`
       - Content order (top to bottom per user decision):
         a. **Starting price**: "From {formatted priceFrom}" in large text — use Intl.NumberFormat same as tiers
         b. **Package count**: "{N} sponsorship tiers available" in muted text
         c. **Primary CTA**: `<MakeOfferDialog>` — renders as a green button (`bg-green-600 hover:bg-green-700 text-white w-full`)
         d. **Secondary CTA**: "Message" button — `<Button variant="outline" className="w-full">` with MessageSquare icon from Lucide. For Phase 3 this is a placeholder (no action, or a toast "Coming in messaging phase")
         e. **Tertiary CTA**: "Schedule Call" button — `<Button variant="ghost" className="w-full">` with Phone icon. Placeholder behavior: shows a toast or alert "Schedule a call by messaging directly" (per Claude's discretion in research)
         f. **Trust badges**: Two items in a muted section:
            - "Verified Seller" with ShieldCheck icon from Lucide
            - "Money Secured" with Lock icon from Lucide
            - Both rendered as `flex items-center gap-2 text-sm text-muted-foreground`
         g. **Divider** (Separator component or `<hr>`)
         h. **Build your own offer teaser**: Brief text "Looking for something custom?" with a link/scroll to the Build Your Own Offer section below

    2. Create `components/property-detail/make-offer-dialog.tsx` — Client Component ("use client"):
       - Accept `propertyId: string` and `propertyName: string` props
       - Renders a `<Dialog>` with `<DialogTrigger asChild>` wrapping the green "Make an Offer" button
       - DialogContent:
         - DialogHeader with DialogTitle: "Make an Offer for {propertyName}"
         - Form with fields:
           a. Amount: `<Input name="amount" type="number" placeholder="Offer amount (USD)" required />`
           b. Terms: `<textarea name="terms" placeholder="Terms, conditions, or notes..." className="min-h-[100px] w-full rounded-md border bg-background px-3 py-2 text-sm" />`
         - Submit button: `<Button type="submit" className="w-full">Submit Offer</Button>`
         - For Phase 3: form submission shows a success toast/alert and closes the dialog. Actual server action for offer creation will be wired in Plan 03-03 or Phase 5.
         - Does NOT navigate away from the page (per user decision)

    3. Update `app/(app)/properties/[slug]/page.tsx`:
       - Import and render `<OfferSidebar>` in the right column (replacing the placeholder div from Plan 03-01)
       - Import and render `<PropertyTiers packages={property.packages} />` in the left column content area (after PropertyAbout)
       - Import and render `<BuildOfferForm />` after PropertyTiers
       - Ensure the sidebar div still has `sticky top-20` and the grid has `items-start`
  </action>
  <verify>
    - Navigate to `/properties/lafc`:
      - Right sidebar shows "From $25,000", "3 sponsorship tiers available"
      - Green "Make an Offer" button is visible
      - "Message" and "Schedule Call" buttons render below
      - Trust badges "Verified Seller" and "Money Secured" show with icons
    - Click "Make an Offer" — modal dialog opens with amount and terms fields
    - Submit the form — success feedback appears, dialog closes
    - Sidebar sticks as you scroll down through the left column content
    - All three sections (tiers, build offer, sidebar) are visible and functional
  </verify>
  <done>Sticky sidebar with all CTAs and trust badges renders in right column; Make an Offer modal opens and accepts input; tiers and build-offer wired into the page layout</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `/properties/lafc` shows all tiers sorted ascending: Community Partner ($25,000), Gold Partner ($55,000), Founding Partner ($125,000)
3. Clicking a tier expands its inclusion list; clicking again collapses it
4. Selected tier has brand-colored border accent
5. Sidebar sticks as user scrolls (verify by scrolling — sidebar stays visible in viewport)
6. Green "Make an Offer" button opens a modal dialog
7. Modal form has amount and terms fields; submitting shows success feedback
8. "Message" and "Schedule Call" buttons are visible in sidebar
9. Trust badges display with appropriate icons
10. "Build your own offer" form renders below tiers with budget, checkboxes, notes
</verification>

<success_criteria>
- Tier pricing section displays vertically sorted by price with click-to-expand inclusions
- No tier ever shows "$0" — handled with "Contact for pricing" fallback
- Sticky sidebar shows minimum spend, three CTAs, and two trust badges
- Make an Offer modal opens without navigation, has amount + terms fields
- Build your own offer form renders below tiers with structured inputs
- Page builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-detail/03-02-SUMMARY.md`
</output>
