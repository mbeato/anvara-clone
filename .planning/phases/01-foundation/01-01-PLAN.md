---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components.json
  - lib/utils.ts
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "npm run dev starts without errors and displays a page"
    - "TypeScript strict mode is enabled"
    - "Tailwind CSS v4 utility classes render correctly"
    - "ShadCN UI components are installable via CLI"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "app/layout.tsx"
      provides: "Root layout shell"
      contains: "RootLayout"
    - path: "app/page.tsx"
      provides: "Home page placeholder"
      min_lines: 5
    - path: "next.config.ts"
      provides: "Next.js configuration with Picsum image host"
      contains: "picsum.photos"
    - path: "components.json"
      provides: "ShadCN UI configuration"
      contains: "shadcn"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "postcss.config.mjs"
      to: "tailwindcss"
      via: "PostCSS plugin"
      pattern: "tailwindcss"
---

<objective>
Scaffold the Next.js project with TypeScript, Tailwind CSS v4, and ShadCN UI. Configure the image host (Picsum) in next.config.ts.

Purpose: Establish the runnable project skeleton that every subsequent phase builds on. After this plan, `npm run dev` works and ShadCN components can be added.

Output: A working Next.js 16 app with Tailwind v4 and ShadCN initialized, configured for Picsum images.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project and configure Tailwind v4 + Picsum images</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, .gitignore</files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --turbopack --yes` from the project root `/Users/vtx/anv`.

IMPORTANT: The project root already has `.git`, `.planning`, `.claude`, and `screenshots` directories. `create-next-app` should populate the remaining structure without disturbing existing files.

After scaffolding:

1. Verify `postcss.config.mjs` uses `@tailwindcss/postcss` (Tailwind v4 pattern). If not, replace its content with:
   ```js
   const config = {
     plugins: {
       "@tailwindcss/postcss": {},
     },
   };
   export default config;
   ```

2. Verify `app/globals.css` has `@import "tailwindcss"` at the top (Tailwind v4 CSS-first pattern). If it uses the old `@tailwind base/components/utilities` directives, replace with `@import "tailwindcss";`.

3. Update `next.config.ts` to add Picsum image host:
   ```typescript
   import type { NextConfig } from "next";

   const nextConfig: NextConfig = {
     images: {
       remotePatterns: [
         {
           protocol: "https",
           hostname: "picsum.photos",
         },
       ],
     },
   };

   export default nextConfig;
   ```

4. Replace `app/page.tsx` with a minimal placeholder:
   ```tsx
   export default function Home() {
     return (
       <main className="flex min-h-screen items-center justify-center">
         <h1 className="text-4xl font-bold">Anvara</h1>
       </main>
     );
   }
   ```

5. Ensure `.gitignore` includes `.env.local` and `node_modules`. Add `.env.local` if missing.

6. Run `npm run dev` briefly (start and kill after confirming no errors) to verify the scaffold works.
  </action>
  <verify>
Run `npm run dev` — server starts on localhost:3000 without errors. Kill after startup. Then run `npx tsc --noEmit` to verify TypeScript compiles cleanly.
  </verify>
  <done>Next.js dev server starts without errors. TypeScript compiles. Tailwind v4 CSS-first config is in place. Picsum image host configured in next.config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize ShadCN UI</name>
  <files>components.json, lib/utils.ts</files>
  <action>
Run `npx shadcn@latest init` from project root. When prompted (or via flags):
- Style: Default
- Base color: Neutral (or Zinc — either is fine)
- CSS variables: Yes

This creates `components.json` at project root and `lib/utils.ts` (with the `cn` utility function).

After init, verify:
1. `components.json` exists and references the correct paths
2. `lib/utils.ts` exists and exports `cn`
3. `app/globals.css` has been updated with ShadCN CSS variables (ShadCN init adds these automatically)

Do NOT install any specific ShadCN components yet — they will be added as needed in later phases.
  </action>
  <verify>
Verify `components.json` exists: `cat components.json`. Verify `lib/utils.ts` exports cn: `grep "export function cn" lib/utils.ts`. Run `npm run dev` again to confirm no regressions.
  </verify>
  <done>`components.json` and `lib/utils.ts` exist. ShadCN CSS variables are in globals.css. Dev server still starts cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts cleanly on localhost:3000
2. `npx tsc --noEmit` passes with zero errors
3. `components.json` exists (ShadCN initialized)
4. `next.config.ts` contains `picsum.photos` in remotePatterns
5. `app/globals.css` uses `@import "tailwindcss"` (v4 pattern)
6. `.gitignore` includes `.env.local`
</verification>

<success_criteria>
The project runs locally with `npm run dev`, TypeScript compiles, Tailwind v4 renders utility classes, ShadCN is initialized and ready for component installation, and Picsum is configured as an allowed image host.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
