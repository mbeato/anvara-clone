---
phase: 06.4-stub-pages-and-tour
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/tour-modal.tsx
  - components/app-sidebar.tsx
  - app/(app)/personalize/page.tsx
  - app/(app)/personalize/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking Show Me Around in the sidebar opens a multi-step modal (not a page navigation)"
    - "Tour modal has 4 steps with progress dots, Back/Next navigation"
    - "Tour resets to step 1 when reopened after closing"
    - "Navigating to /personalize shows a settings/preferences page with form-style sections"
    - "A loading skeleton appears when navigating to /personalize"
  artifacts:
    - path: "components/tour-modal.tsx"
      provides: "Multi-step tour modal with 4 steps"
      contains: "TOUR_STEPS"
      min_lines: 50
    - path: "components/app-sidebar.tsx"
      provides: "Modified sidebar with tour modal integration"
      contains: "setTourOpen"
    - path: "app/(app)/personalize/page.tsx"
      provides: "Settings/preferences page"
      contains: "Personalize"
    - path: "app/(app)/personalize/loading.tsx"
      provides: "Skeleton loading state"
      contains: "Skeleton"
  key_links:
    - from: "components/app-sidebar.tsx"
      to: "components/tour-modal.tsx"
      via: "import TourModal, useState for open state"
      pattern: "TourModal.*open.*tourOpen"
    - from: "components/app-sidebar.tsx"
      to: "Show Me Around button"
      via: "onClick handler instead of Link href"
      pattern: "onClick.*setTourOpen"
---

<objective>
Build the multi-step platform tour modal and Personalize settings page.

Purpose: The "Show Me Around" sidebar item currently links to `/tour` which 404s — this plan converts it to a button that opens a multi-step modal walkthrough. The `/personalize` sidebar link also 404s — this plan adds a static settings/preferences page.

Output: Working tour modal triggered from sidebar, functional Personalize page with form sections, both with loading skeletons.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/app-sidebar.tsx
@components/ui/dialog.tsx
@components/ui/card.tsx
@components/ui/separator.tsx
@.planning/phases/06.4-stub-pages-and-tour/06.4-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tour modal component and integrate with sidebar</name>
  <files>
    components/tour-modal.tsx
    components/app-sidebar.tsx
  </files>
  <action>
    1. Create `components/tour-modal.tsx` as a "use client" component:

    **TOUR_STEPS array (4 steps):**
    - Step 1: "Welcome to Anvara" — intro to the sponsorship marketplace platform. Icon: Sparkles.
    - Step 2: "Browse the Marketplace" — discover sponsorship opportunities, filter by category/region/budget. Icon: Store.
    - Step 3: "Track Your Performance" — analytics dashboard with spend, impressions, conversion tracking. Icon: ChartArea.
    - Step 4: "AI-Powered Discovery" — use Anvara AI to find perfect-fit properties with natural language. Icon: Sparkles (or use a different icon like Briefcase for "Close Deals Faster").

    Each step has: title, description (2-3 sentences), icon (LucideIcon reference — NOT iconName string since this stays client-side).

    **TourModal component:**
    - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`
    - Internal state: `step` (number, 0-indexed)
    - Uses Dialog from ShadCN (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter)
    - DialogContent: `className="sm:max-w-md"` for narrow modal
    - Renders current step's icon (large, centered, in a rounded background div with primary/10 color), title, and description
    - Progress dots: horizontal row of small circles, active step = `bg-primary`, others = `bg-muted`. Use `TOUR_STEPS.map((_, i) => ...)` with conditional class.
    - Footer: Back button (outline variant, hidden on step 0), Next button (default variant), "Get Started" on last step instead of "Next"
    - **Critical: handleClose function must call BOTH `onOpenChange(false)` AND `setStep(0)`** — this ensures reopening always starts at step 1
    - Back: `setStep(s => s - 1)`, Next: `setStep(s => s + 1)`

    2. Modify `components/app-sidebar.tsx`:

    **Add imports:**
    - `import { useState } from "react"` (add to existing imports)
    - `import { TourModal } from "@/components/tour-modal"`

    **Change the "Show Me Around" footer item from Link to onClick button:**

    Currently, footerItems is an array and all items are rendered identically with `asChild` + `Link`. The "Show Me Around" item must be handled specially.

    Strategy: Remove "Show Me Around" from the `footerItems` array. Render it manually in the footer section between Personalize and Logout, using `SidebarMenuButton` WITHOUT `asChild`, with `onClick={() => setTourOpen(true)}`. Keep the same icon (CircleHelp) and styling (`[&>svg]:mx-4`).

    **Add state and render TourModal:**
    - Add `const [tourOpen, setTourOpen] = useState(false)` inside AppSidebar
    - Wrap the return in a `<>` fragment
    - Render `<TourModal open={tourOpen} onOpenChange={setTourOpen} />` OUTSIDE `<Sidebar>` as a sibling (prevents z-index/portal issues)

    **Resulting footer structure:**
    ```
    Personalize (Link, from footerItems minus Show Me Around)
    Show Me Around (button, onClick opens tour)
    Logout (Link, from footerItems minus Show Me Around)
    Account avatar (existing, unchanged)
    ```
  </action>
  <verify>
    - `components/tour-modal.tsx` exports TourModal with 4 steps
    - `components/app-sidebar.tsx` imports TourModal and uses useState for tourOpen
    - "Show Me Around" item has `onClick` not `href`
    - TourModal is rendered outside `<Sidebar>` element
    - handleClose resets step to 0
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    Clicking "Show Me Around" opens a 4-step modal tour with progress dots and Back/Next navigation; closing and reopening resets to step 1
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Personalize settings page and loading skeleton</name>
  <files>
    app/(app)/personalize/page.tsx
    app/(app)/personalize/loading.tsx
  </files>
  <action>
    1. Create `app/(app)/personalize/page.tsx` as a plain Server Component (NOT async — no DB queries):
    - `export const metadata = { title: "Personalize | Anvara" }`
    - Page heading: "Personalize" with subtitle "Customize your Anvara experience"
    - Layout: `space-y-6 p-6 max-w-2xl mx-auto` (narrower than other pages since it's a form)

    **Settings sections (3-4 Card components):**

    Section 1: "Notification Preferences"
    - Card with CardHeader (title + description) and CardContent
    - 3-4 rows with label + description on left, a visual toggle indicator on right
    - Rows: "Email Notifications" (Campaign updates and reports), "Deal Alerts" (New offers and negotiations), "Weekly Digest" (Performance summary), "Marketing" (Product news and features)
    - Since no Switch component exists, use a styled div that looks like a toggle (rounded-full bg with circle) or use a simple Checkbox from ShadCN. Or render a small Badge that says "On"/"Off". Keep it static/visual — no interactivity needed.

    Section 2: "Display Preferences"
    - Rows: "Default View" (Grid/List — show as text or Select), "Results Per Page" (show as text "12"), "Currency" (show as text "USD")
    - Use Separator between rows for clean dividers

    Section 3: "Marketplace Interests"
    - Show interest categories as Badge chips (Music Festivals, Food & Beverage, Sports, Tech Conferences, etc.)
    - Some badges styled as "selected" (default variant) and others as "unselected" (outline variant)
    - Static — no toggle logic needed

    Section 4: "Account"
    - Company name (VertikalX), email, timezone
    - Display as read-only styled text fields (label above, value below with muted background)

    All sections use Card, CardHeader, CardTitle, CardDescription, CardContent from ShadCN. Use Separator between sections if needed. Keep everything static — this is a prototype page showing what settings WOULD look like.

    2. Create `app/(app)/personalize/loading.tsx`:
    - Import Skeleton
    - Render 3-4 card-shaped skeletons (h-48) stacked vertically in max-w-2xl mx-auto layout
    - Follow dashboard loading.tsx pattern
  </action>
  <verify>
    - `app/(app)/personalize/page.tsx` is NOT async and renders 3-4 settings sections
    - Uses Card, Badge, Separator from ShadCN
    - loading.tsx renders Skeleton components
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    /personalize page shows a polished settings/preferences UI with notification, display, interests, and account sections; loading skeleton appears on navigation
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. Clicking "Show Me Around" in sidebar opens a modal (does NOT navigate to /tour)
3. Tour modal shows 4 steps with progress dots, Back/Next buttons
4. Closing tour and reopening starts at step 1 (not where you left off)
5. Navigate to `/personalize` — page loads with 3-4 settings sections in Card components
6. Loading skeleton appears briefly on /personalize navigation
</verification>

<success_criteria>
- Show Me Around triggers a modal overlay, not page navigation
- Tour has 4 steps, progress dots, Back/Next, resets on reopen
- Personalize page shows realistic settings UI with notification/display/interests/account sections
- All new pages have loading.tsx skeletons
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06.4-stub-pages-and-tour/06.4-02-SUMMARY.md`
</output>
