---
phase: 06.4-stub-pages-and-tour
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/stub-data.ts
  - app/(app)/campaigns/page.tsx
  - app/(app)/campaigns/_components/campaigns-table.tsx
  - app/(app)/campaigns/_components/campaigns-stats.tsx
  - app/(app)/campaigns/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /campaigns shows a page with mock campaign data in a table"
    - "Campaign table displays names, properties, statuses, spend, and impressions"
    - "Campaign KPI cards show summary metrics at the top of the page"
    - "A loading skeleton appears during page load"
  artifacts:
    - path: "lib/data/stub-data.ts"
      provides: "Deterministic mock data for Campaigns and Deals"
      exports: ["getMockCampaigns", "getMockDeals", "MockCampaign", "MockDeal"]
    - path: "app/(app)/campaigns/page.tsx"
      provides: "Server Component campaigns page"
      contains: "getMockCampaigns"
    - path: "app/(app)/campaigns/_components/campaigns-table.tsx"
      provides: "Client component rendering campaign data table"
      min_lines: 30
    - path: "app/(app)/campaigns/_components/campaigns-stats.tsx"
      provides: "KPI summary cards for campaigns"
      min_lines: 15
    - path: "app/(app)/campaigns/loading.tsx"
      provides: "Skeleton loading state"
      contains: "Skeleton"
  key_links:
    - from: "app/(app)/campaigns/page.tsx"
      to: "lib/data/stub-data.ts"
      via: "import getMockCampaigns"
      pattern: "getMockCampaigns"
    - from: "app/(app)/campaigns/page.tsx"
      to: "app/(app)/campaigns/_components/campaigns-table.tsx"
      via: "import CampaignsTable"
      pattern: "CampaignsTable"
---

<objective>
Create the stub data layer and Campaigns page with a realistic campaign table, KPI summary cards, and loading skeleton.

Purpose: The `/campaigns` sidebar link currently 404s. This plan creates the mock data file shared across stub pages and the complete Campaigns page with table view showing campaign names, linked properties, statuses, spend, and impressions.

Output: `lib/data/stub-data.ts` with all mock data (campaigns + deals), fully functional `/campaigns` page with table and KPI cards, loading skeleton.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/data/analytics.ts
@app/(app)/dashboard/page.tsx
@app/(app)/dashboard/loading.tsx
@app/(app)/dashboard/_components/kpi-card.tsx
@app/(app)/dashboard/_components/recent-activity-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub data layer and campaigns page with KPI cards</name>
  <files>
    lib/data/stub-data.ts
    app/(app)/campaigns/page.tsx
    app/(app)/campaigns/_components/campaigns-stats.tsx
  </files>
  <action>
    1. Create `lib/data/stub-data.ts` with ALL mock data for both Campaigns and Deals (Deals data will be consumed by Plan 03):

    **MockCampaign interface:** id, name, property, status ("active" | "paused" | "completed"), startDate, endDate, budget, spend, impressions, clicks
    **getMockCampaigns():** Return 6-7 hardcoded rows with realistic data. Use property names from existing seed data (Coachella, Austin Food & Wine, Jazz in the Park, X Games, etc.). Mix statuses: 2 active, 1 paused, 2 completed, 1 active. Vary budgets ($5K–$45K), spend (some under budget, some near budget), impressions (150K–900K), clicks (3K–15K). Dates should span Jan–Jun 2026. NO Math.random() — all hardcoded deterministic values.

    **MockDeal interface:** id, property, stage ("prospect" | "negotiating" | "agreed" | "live"), value, contact, lastActivity, category
    **getMockDeals():** Return 8-10 hardcoded rows across all 4 stages (2-3 per stage). Use property names from seed data. Vary values ($3K–$50K). Contact names should be realistic (Morgan Lee, Sarah Chen, etc.). lastActivity dates in Feb 2026.

    **getCampaignStats():** Derive 4 KPI objects from the campaigns array: Total Spend (sum of spend), Active Campaigns (count active), Avg. Impressions (average), Total Clicks (sum of clicks). Return array of { label, value (formatted string), trend (percentage number), iconName } matching the KpiData pattern from analytics.ts.

    2. Create `app/(app)/campaigns/_components/campaigns-stats.tsx`:
    - Import KpiCard from the dashboard's existing `_components/kpi-card` component (reuse, don't recreate)
    - Accept `stats` prop (array of KPI objects)
    - Render in a 2x2 / 4-col grid matching dashboard layout
    - Map iconName strings to Lucide icons (same pattern as dashboard page.tsx)

    3. Create `app/(app)/campaigns/page.tsx` as a Server Component:
    - `export const metadata = { title: "Campaigns | Anvara" }`
    - Import getMockCampaigns and getCampaignStats from stub-data
    - Import CampaignsStats and CampaignsTable (Task 2)
    - Page layout: heading "Campaigns" with subtitle, stats cards row, then table
    - Follow dashboard page.tsx layout pattern (space-y-6 p-6 max-w-7xl mx-auto)
    - NOT async — no DB queries, just function calls
  </action>
  <verify>
    - `lib/data/stub-data.ts` exports getMockCampaigns, getMockDeals, getCampaignStats
    - `app/(app)/campaigns/page.tsx` imports from stub-data and renders CampaignsStats
    - No `Math.random()` anywhere in stub-data.ts
    - TypeScript compiles without errors
  </verify>
  <done>
    stub-data.ts exists with typed campaign and deal mock data; campaigns page renders heading and KPI cards
  </done>
</task>

<task type="auto">
  <name>Task 2: Create campaigns table component and loading skeleton</name>
  <files>
    app/(app)/campaigns/_components/campaigns-table.tsx
    app/(app)/campaigns/loading.tsx
  </files>
  <action>
    1. Create `app/(app)/campaigns/_components/campaigns-table.tsx`:
    - "use client" component (for potential future sorting/filtering interactivity)
    - Accept `data` prop typed as MockCampaign[]
    - Use ShadCN Table components (Table, TableHeader, TableRow, TableHead, TableBody, TableCell) — check if these exist at components/ui/table.tsx, if not use a styled HTML table with Tailwind
    - Columns: Campaign Name, Property, Status, Budget, Spend, Impressions, Clicks
    - Status column: Use Badge component — "active" = default variant, "paused" = secondary, "completed" = outline
    - Budget/Spend columns: Format as USD with Intl.NumberFormat or template literal ($XX,XXX)
    - Impressions/Clicks: Format with locale string (commas)
    - Table should have alternating row styles or hover states for polish

    2. Create `app/(app)/campaigns/loading.tsx`:
    - Import Skeleton from components/ui/skeleton
    - Match the page layout: 4 KPI card skeletons (h-28) in 2x2/4-col grid, then a full-width table skeleton (h-96)
    - Follow dashboard loading.tsx pattern exactly
  </action>
  <verify>
    - `npx tsc --noEmit` passes (or at minimum, no errors in campaigns files)
    - campaigns-table.tsx renders a table with Badge status indicators
    - loading.tsx renders Skeleton components matching page layout
  </verify>
  <done>
    Campaigns table component renders all 7 columns with formatted data and status badges; loading skeleton matches page structure
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. Navigate to `/campaigns` — page loads with heading, 4 KPI cards, and data table
3. Table shows 6-7 rows with campaign names, properties, status badges, formatted currency and numbers
4. Refreshing the page shows the loading skeleton briefly before content
5. No `Math.random()` in any new file
</verification>

<success_criteria>
- /campaigns page renders a complete campaign management view with KPI summary and data table
- All data is deterministic (no randomness)
- Loading skeleton appears on navigation
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06.4-stub-pages-and-tour/06.4-01-SUMMARY.md`
</output>
