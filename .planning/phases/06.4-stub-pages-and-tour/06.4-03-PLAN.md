---
phase: 06.4-stub-pages-and-tour
plan: 03
type: execute
wave: 2
depends_on: ["06.4-01"]
files_modified:
  - app/(app)/deals/page.tsx
  - app/(app)/deals/_components/deals-pipeline.tsx
  - app/(app)/deals/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /deals shows a pipeline/stage-grouped view with deal cards"
    - "Deals are organized across 4 stages: Prospecting, Negotiating, Agreed, Live"
    - "Each deal card shows property name, value, contact, and last activity"
    - "A loading skeleton appears during page load"
    - "Build passes cleanly after all 3 plans"
  artifacts:
    - path: "app/(app)/deals/page.tsx"
      provides: "Server Component deals page"
      contains: "getMockDeals"
    - path: "app/(app)/deals/_components/deals-pipeline.tsx"
      provides: "Client component rendering stage-grouped deal cards"
      contains: "stage"
      min_lines: 40
    - path: "app/(app)/deals/loading.tsx"
      provides: "Skeleton loading state"
      contains: "Skeleton"
  key_links:
    - from: "app/(app)/deals/page.tsx"
      to: "lib/data/stub-data.ts"
      via: "import getMockDeals"
      pattern: "getMockDeals"
    - from: "app/(app)/deals/page.tsx"
      to: "app/(app)/deals/_components/deals-pipeline.tsx"
      via: "import DealsPipeline"
      pattern: "DealsPipeline"
---

<objective>
Create the Deals pipeline page with a Kanban-style stage-grouped card layout.

Purpose: The `/deals` sidebar link currently 404s. This plan creates a visually distinct deals page showing deal cards grouped by pipeline stage (Prospecting, Negotiating, Agreed, Live) — different from the Campaigns table view for visual variety.

Output: Fully functional `/deals` page with stage-grouped pipeline cards, loading skeleton. After this plan, run build verification to confirm entire phase passes.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/data/stub-data.ts
@.planning/phases/06.4-stub-pages-and-tour/06.4-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deals pipeline page with stage-grouped cards</name>
  <files>
    app/(app)/deals/page.tsx
    app/(app)/deals/_components/deals-pipeline.tsx
  </files>
  <action>
    1. Create `app/(app)/deals/_components/deals-pipeline.tsx` as a "use client" component:

    **Stage definitions:**
    Define a STAGES array with 4 objects: { key: "prospect", label: "Prospecting", color: "bg-blue-500" }, { key: "negotiating", label: "Negotiating", color: "bg-amber-500" }, { key: "agreed", label: "Agreed", color: "bg-emerald-500" }, { key: "live", label: "Live", color: "bg-primary" }

    **DealsPipeline component:**
    - Accept `deals` prop typed as MockDeal[] (import type from stub-data.ts)
    - Group deals by stage: for each stage in STAGES, filter deals where `deal.stage === stage.key`
    - Render as a 4-column CSS grid (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4`)
    - Each column: stage header (colored dot + label + count badge) followed by stacked Card components

    **Individual deal card (inside same file or as a local component):**
    - Card with CardHeader: property name (font-medium, truncate for long names)
    - CardContent: value formatted as USD, contact name, lastActivity date, category as Badge (outline variant)
    - Subtle left border color matching the stage color (`border-l-2` with the stage's color class)

    2. Create `app/(app)/deals/page.tsx` as a Server Component:
    - `export const metadata = { title: "Deals | Anvara" }`
    - Import getMockDeals from `@/lib/data/stub-data`
    - Import DealsPipeline component
    - NOT async — just call getMockDeals()
    - Page heading: "Deals" with subtitle "Your sponsorship pipeline"
    - Summary stats row: Total pipeline value (sum of all deal values, formatted as USD), deal count, grouped counts by stage
    - Below stats: DealsPipeline component receiving deals data
    - Layout: space-y-6 p-6 max-w-7xl mx-auto
  </action>
  <verify>
    - deals/page.tsx imports getMockDeals and renders DealsPipeline
    - deals-pipeline.tsx groups deals into 4 stage columns
    - Each deal card shows property, value, contact, lastActivity
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    /deals page renders a pipeline view with deal cards organized by stage (Prospecting, Negotiating, Agreed, Live)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create deals loading skeleton and verify full build</name>
  <files>
    app/(app)/deals/loading.tsx
  </files>
  <action>
    1. Create `app/(app)/deals/loading.tsx`:
    - Import Skeleton from components/ui/skeleton
    - Match the pipeline layout: heading skeleton, stats row skeleton (4 small blocks), then 4-column grid with 2-3 card skeletons per column (h-32 each)
    - Follow dashboard loading.tsx pattern

    2. Run `npm run build` to verify the ENTIRE phase builds cleanly:
    - All 3 new pages (campaigns, deals, personalize) compile
    - Tour modal component compiles
    - Modified app-sidebar compiles
    - No TypeScript errors, no build warnings related to new files
    - If build fails, fix the issues in the affected files
  </action>
  <verify>
    - `npm run build` passes with exit code 0
    - loading.tsx renders Skeleton components matching pipeline layout
    - No TypeScript errors in any new or modified file
  </verify>
  <done>
    Deals loading skeleton exists; `npm run build` passes cleanly confirming all phase 6.4 files compile correctly
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors (this is the final build check for the entire phase)
2. Navigate to `/deals` — page loads with heading, summary stats, and 4-column pipeline
3. Each stage column has a header with colored dot and count, followed by deal cards
4. Deal cards show property name, value (USD), contact, last activity, category badge
5. Loading skeleton appears on /deals navigation
6. All sidebar links work: /campaigns (table), /deals (pipeline), /personalize (settings), Show Me Around (modal)
</verification>

<success_criteria>
- /deals page renders a visually distinct pipeline view (not a table — different from Campaigns)
- 4 stages visible: Prospecting, Negotiating, Agreed, Live
- Deal cards show all relevant info (property, value, contact, activity, category)
- Loading skeleton matches page layout
- npm run build passes with ALL new phase 6.4 files
</success_criteria>

<output>
After completion, create `.planning/phases/06.4-stub-pages-and-tour/06.4-03-SUMMARY.md`
</output>
