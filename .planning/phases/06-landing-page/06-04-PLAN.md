---
phase: 06-landing-page
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/_components/landing/for-brands-rightsholders.tsx
  - app/_components/landing/testimonial.tsx
  - app/_components/landing/faq-section.tsx
  - app/_components/landing/final-cta.tsx
  - app/_components/landing/landing-footer.tsx
autonomous: true

must_haves:
  truths:
    - "For Brands / For Rightsholders section shows two columns with brand logo grids and CTAs"
    - "Testimonial section shows exact GoPuff quote with attribution to Edan Mayron"
    - "FAQ section has 6 accordion items that expand/collapse with only one open at a time"
    - "Final CTA section has blue banner with 'Skip the Decks, Seal the deal' heading and smooth continuous-scroll marquee of property cards"
    - "Footer has links, newsletter input (visual only), and large 'anvara' watermark text"
  artifacts:
    - path: "app/_components/landing/for-brands-rightsholders.tsx"
      provides: "Two-column logo grids with CTAs"
      exports: ["ForBrandsRightsholders"]
    - path: "app/_components/landing/testimonial.tsx"
      provides: "GoPuff testimonial quote"
      exports: ["Testimonial"]
    - path: "app/_components/landing/faq-section.tsx"
      provides: "6 accordion FAQ items"
      exports: ["FaqSection"]
    - path: "app/_components/landing/final-cta.tsx"
      provides: "Blue banner with marquee carousel"
      exports: ["FinalCTA"]
    - path: "app/_components/landing/landing-footer.tsx"
      provides: "Footer with links and watermark"
      exports: ["LandingFooter"]
  key_links:
    - from: "app/_components/landing/faq-section.tsx"
      to: "components/ui/accordion.tsx"
      via: "ShadCN Accordion component"
      pattern: "Accordion"
    - from: "app/_components/landing/final-cta.tsx"
      to: "app/_components/landing/landing-property-card.tsx"
      via: "Marquee carousel of LandingPropertyCards"
      pattern: "LandingPropertyCard"
---

<objective>
Build the bottom 5 sections of the landing page — For Brands/Rightsholders (logo grids), Testimonial (GoPuff quote), FAQ (6 accordion items), Final CTA (blue banner + marquee), and Footer (links + watermark).

Purpose: These sections complete the page with social proof, FAQ answers, a final conversion push, and professional footer — all faithful to Anvara's current site.

Output: Five section components ready for page assembly.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@app/_components/landing/section-reveal.tsx
@app/_components/landing/landing-property-card.tsx
@app/globals.css
@components/ui/accordion.tsx

Reference screenshots:
- screenshots/Landing7.png — For Brands / For Rightsholders (two-column logo grids)
- screenshots/Landing8.png — Testimonial (GoPuff quote) + FAQ section header
- screenshots/Landing9.png — FAQ items expanded + Final CTA blue banner top
- screenshots/Landing10.png — Final CTA (blue banner with property card marquee)
- screenshots/Landing11.png — Footer (links, newsletter, "anvara" watermark)
</context>

<tasks>

<task type="auto">
  <name>Task 1: For Brands/Rightsholders section and Testimonial</name>
  <files>
    app/_components/landing/for-brands-rightsholders.tsx
    app/_components/landing/testimonial.tsx
  </files>
  <action>
**1. For Brands / For Rightsholders (`for-brands-rightsholders.tsx`):**
Reference screenshots Landing7.png and Landing8.png.

Layout: Two-column grid on desktop (side by side), stacks on mobile.

Each column is a card with:
- Heading: "For Brands" (left) / "For Rightsholders" (right)
- Body text: "Anvara puts premium sponsorships just a few clicks away. Built for speed. Free to explore."
- Logo grid: 2 rows of 4-5 logos each (grayscale, small icons/text)
- CTA button at bottom: "Anvara for Brands" (left, blue) / "Anvara for Rightsholders" (right, blue) — both link to /browse

**Brand logos (left column)** — per CONTEXT.md, exact list:
gopuff, Coca-Cola, Snapchat, McDonald's, Mastercard, Kalshi, Brex, TikTok, Ally, LEGO

Render logos as styled text labels in a grid. Use recognizable brand styling:
- "gopuff" — lowercase, bold
- "Coca-Cola" — script/italic style
- Snapchat ghost icon (use 👻 or a simple SVG ghost shape)
- "M" in golden arch style for McDonald's (or just "McDonald's")
- "mastercard" — two overlapping circles
- "Kalshi" — bold sans
- "Brex" — with the Brex arrow icon (≡ "⊟ Brex")
- "TikTok" — with music note
- "ally" — lowercase bold
- "LEGO" — uppercase bold red

Apply `opacity-60 grayscale` to the logo container.

**Rightsholder logos (right column)** — per CONTEXT.md:
LAFC, Serie A, SXSW, We Belong Here, ATP Tour, and others from screenshots

From screenshots (Landing7.png, Landing8.png): LAFC shield, Serie A "S" logo, SXSW text, "WE BELONG HERE" text, ATP TOUR text, plus additional logos like PGA, NASCAR, Rockies

Render as styled text labels with recognizable formatting.

Both columns: Wrap in a card-like container with rounded corners, light gray background on hover or subtle border. Use SectionReveal.

**2. Testimonial (`testimonial.tsx`):**
Reference screenshot Landing8.png.

Simple centered quote section:
- Light gray background or subtle top/bottom border lines (from screenshot — horizontal rules above and below)
- Italic quote text, centered: *"It's like having a full-service agency available 24/7 that responds instantly and isn't biased—but 100x smarter."*
- Attribution: "— Edan Mayron, GoPuff" (per CONTEXT.md, exact quote)
- Keep it minimal — single line quote, no extra decoration
- Moderate padding (`py-12 px-4`)
- `text-muted-foreground` for the quote, `text-sm` for attribution

Wrap in SectionReveal.
  </action>
  <verify>
`npx tsc --noEmit` passes. Both components export correctly. Testimonial contains the exact GoPuff quote. Brand list matches CONTEXT.md specifications.
  </verify>
  <done>
ForBrandsRightsholders renders two-column logo grids with brand (gopuff, Coca-Cola, etc.) and rightsholder (LAFC, Serie A, etc.) logos in grayscale with CTAs. Testimonial displays the exact GoPuff quote with proper attribution.
  </done>
</task>

<task type="auto">
  <name>Task 2: FAQ section with accordion</name>
  <files>
    app/_components/landing/faq-section.tsx
  </files>
  <action>
Create `app/_components/landing/faq-section.tsx` as a "use client" component. Reference screenshots Landing8.png and Landing9.png.

**Header:**
- Badge: "FAQ" (small pill, centered)
- Heading: "Frequently Asked Questions" (large, centered)
- Subtext: "Still have questions? Reach out to us via our email or support line." (centered, muted)

**Accordion items (6 total, per CONTEXT.md):**
Use the ShadCN Accordion component (`@/components/ui/accordion`). Set `type="single"` and `collapsible` to ensure only one item open at a time (per research Pitfall 7).

The 6 FAQ items:

1. **"What is Anvara?"**
   Answer: "Anvara is a marketplace that connects brands and rights holders to buy and sell sponsorships, events, and real-world media opportunities. Whether you're looking to sponsor a music festival, partner with a sports team, or access unique media placements, Anvara makes it simple to discover, negotiate, and execute deals—all in one platform."

2. **"How does payment work?"**
   Answer: "Anvara facilitates secure payments between brands and rights holders. Once both parties agree on terms, payments are processed through our platform with built-in escrow protection. We support multiple payment methods and provide detailed invoicing for every transaction."

3. **"What types of opportunities are available?"**
   Answer: "Our marketplace features a wide range of sponsorship opportunities including sports partnerships, music festivals, food and beverage events, tech conferences, cultural experiences, and more. From title sponsorships to on-site activations, you'll find opportunities that match your brand's audience and budget."

4. **"Can agencies use Anvara?"**
   Answer: "Absolutely. Anvara is built for agencies managing multiple brand relationships. Our platform lets you browse opportunities, manage campaigns, and track performance across all your clients from a single dashboard. Agency-specific features include multi-brand management and consolidated reporting."

5. **"Is there support if I need help?"**
   Answer: "Yes. Our team is available via email and our in-platform support chat. Whether you need help finding the right opportunity, negotiating terms, or troubleshooting an issue, we're here to help. Premium accounts also get access to a dedicated account manager."

6. **"Is Anvara available internationally?"**
   Answer: "Anvara currently focuses on North American opportunities with a growing selection of international listings. We're actively expanding into European, Asian, and Latin American markets. If you have specific international needs, reach out and we'll help you find the right match."

**Styling:**
- Each accordion item: `border-b border-border` with `py-4`
- Trigger has the question text and a chevron icon that rotates on open
- Content fades in on expand
- Max width ~700px, centered on page
- SectionReveal on the outer container
  </action>
  <verify>
`npx tsc --noEmit` passes. Component exports FaqSection. Accordion uses `type="single" collapsible` props. All 6 questions are present. Confirm `components/ui/accordion.tsx` exists (installed in Plan 01). If not present, run `npx shadcn@latest add accordion` before proceeding.
  </verify>
  <done>
FAQ section renders 6 accordion items with only one expandable at a time. Questions match CONTEXT.md specifications. Answers match Anvara's tone and content.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final CTA banner with marquee and Landing Footer</name>
  <files>
    app/_components/landing/final-cta.tsx
    app/_components/landing/landing-footer.tsx
  </files>
  <action>
**1. Final CTA (`final-cta.tsx`):**
Reference screenshot Landing10.png.

Blue banner section with property card marquee.

Layout:
- Full-width section with `bg-primary` (Anvara blue) background
- Two-column on desktop: left side text, right side scrolling property cards
- Generous padding (`py-16 px-8`)

Left side:
- Heading: "Skip the Decks, Seal the deal" — large, white, bold
- Body: "Anvara puts premium sponsorships just a few clicks away. Built for speed. Free to explore." — white/80 opacity
- CTA button: "Get Started" — white bg with primary text (`bg-white text-primary`), links to /browse, with arrow →

Right side — Marquee carousel:
- Smooth continuous scroll of all 12 property cards using CSS `animate-marquee` (from globals.css keyframes added in 06-01)
- Use LandingPropertyCard for each card
- Duplicate the array (`[...properties, ...properties]`) to create seamless loop
- Container: `overflow-hidden` with inner `flex gap-4 animate-marquee w-max`
- The marquee should NOT have hover-pause (continuous flow per CONTEXT.md "smooth continuous-scroll")
- Cards on the blue background need adequate contrast — the LandingPropertyCard already has gradient overlay so it should work

Component signature:
```tsx
interface FinalCTAProps {
  properties: Array<{ id: string; name: string; category: string; subcategory: string | null; region: string; heroImage: string }>;
}
```

Wrap in SectionReveal (the reveal animation fires once, then marquee runs continuously).

**2. Landing Footer (`landing-footer.tsx`):**
Reference screenshot Landing11.png.

Layout:
- Top section: tagline split across the width — left: "Opportunities don't chase you." right: "Discover them with Anvara." (from screenshot)
- Large "anvara" watermark text — very large (text-[120px] or similar), light gray/faded (`text-muted/20`), centered, using a bold font weight. This is the signature visual from the Anvara footer.
- Below watermark: a dark background section (bg-slate-950 or bg-zinc-950) with:
  - A scrolling image/card strip (similar to hero carousel but smaller, optional — can be simplified to a single banner image)
  - Footer link columns:
    - Column 1 "Product": Marketplace, Intelligence, Analytics, Pricing
    - Column 2 "For Rightsholders": List Property, Dashboard, Resources
    - Column 3 "For Agencies": Agency Tools, Contact Us
    - Column 4 "Company": About, Careers, Privacy
  - All footer links are visual only (no functional routes) — use `href="#"` or simply `<span>` elements
  - Newsletter input: "Enter your email" input + "Get Started" button (visual only per CONTEXT.md deferred — newsletter form not functional)
  - Small text: "We won't spam you. Only the important stuff."
  - Bottom bar: "© 2025 Anvara" on left, social links on right (LinkedIn, etc. — text links, visual only)
  - Add "Prototype by Max Beato" attribution (per PROJECT.md requirement — footer on every page, but landing footer is separate from app footer)

Use `text-white` and `text-white/60` for footer text on dark background.
No SectionReveal needed on footer (it's at the bottom, always visible by the time user scrolls there).
  </action>
  <verify>
`npx tsc --noEmit` passes. Both components export correctly. Final CTA uses `animate-marquee` CSS class. Footer contains "Prototype by Max Beato". Newsletter input is visual-only (no form submission handler).
  </verify>
  <done>
FinalCTA renders blue banner with "Skip the Decks, Seal the deal" heading and smooth marquee carousel of all 12 property cards. LandingFooter renders link columns, newsletter input (visual only), "anvara" watermark text, and "Prototype by Max Beato" attribution.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All five component files exist under `app/_components/landing/`
3. ForBrandsRightsholders has two columns with brand logos matching CONTEXT.md lists
4. Testimonial has exact GoPuff quote: "It's like having a full-service agency available 24/7..."
5. FAQ has 6 accordion items using ShadCN Accordion with `type="single" collapsible`
6. FinalCTA uses `animate-marquee` with duplicated property array for seamless loop
7. LandingFooter has "Prototype by Max Beato" and newsletter input is visual-only
8. All CTAs link to `/browse`
</verification>

<success_criteria>
- All 5 remaining section components compile and export correctly
- FAQ accordion allows only one item open at a time
- Marquee carousel in Final CTA scrolls continuously
- Footer faithful to Anvara's current site with proper attribution
- Newsletter form is visual only (no submission handler)
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-04-SUMMARY.md`
</output>
