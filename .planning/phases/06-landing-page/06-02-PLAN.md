---
phase: 06-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/_components/landing/hero-section.tsx
  - app/_components/landing/brand-logos-bar.tsx
  - app/_components/landing/what-is-anvara.tsx
  - app/_components/landing/access-market.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section displays video background with dark overlay, headline, CTA button, and scrolling property cards"
    - "CTA button labeled 'Try Anvara Free' links to /browse"
    - "Hero property carousel auto-advances every 3 seconds with jump-snap behavior"
    - "Brand logos bar shows micro1, Legendz, Delta, gopuff logos in grayscale"
    - "What is Anvara section has rotating text cycling through event types"
    - "Access the Whole Market section lists property names with a 'Check the Market' CTA"
  artifacts:
    - path: "app/_components/landing/hero-section.tsx"
      provides: "Hero with video bg, headline, CTA, snap carousel"
      exports: ["HeroSection"]
    - path: "app/_components/landing/brand-logos-bar.tsx"
      provides: "Brand logos bar with grayscale treatment"
      exports: ["BrandLogosBar"]
    - path: "app/_components/landing/what-is-anvara.tsx"
      provides: "Rotating text section with 3D token illustrations"
      exports: ["WhatIsAnvara"]
    - path: "app/_components/landing/access-market.tsx"
      provides: "Property list with Check the Market CTA"
      exports: ["AccessMarket"]
  key_links:
    - from: "app/_components/landing/hero-section.tsx"
      to: "app/_components/landing/landing-property-card.tsx"
      via: "LandingPropertyCard in carousel"
      pattern: "LandingPropertyCard"
    - from: "app/_components/landing/hero-section.tsx"
      to: "/browse"
      via: "CTA button Link"
      pattern: "href.*browse"
---

<objective>
Build the top 4 sections of the landing page — Hero (video bg + headline + CTA + snap carousel), Brand Logos Bar, What is Anvara (rotating text + 3D tokens), and Access the Whole Market (property list + CTA).

Purpose: These are the first sections visitors see. The hero makes the strongest first impression with video, bold typography, and scrolling property cards. The brand logos build trust. The rotating text section explains Anvara's value proposition.

Output: Four section components ready to be assembled into page.tsx.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@app/_components/landing/section-reveal.tsx
@app/_components/landing/landing-property-card.tsx
@app/globals.css
@components/ui/carousel.tsx

Reference screenshots:
- screenshots/Landing1.png — Hero section (video bg, headline, CTA, property cards at bottom)
- screenshots/Landing2.png — Brand logos bar (micro1, Legendz, Delta, gopuff) + What is Anvara section
- screenshots/Landing3.png — Access the Whole Market section (property list, Check the Market CTA)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section with video background, headline, CTA, and snap carousel</name>
  <files>
    app/_components/landing/hero-section.tsx
  </files>
  <action>
Create `app/_components/landing/hero-section.tsx` as a "use client" component. Reference screenshot Landing1.png for visual target.

**Video Background:**
- Use a single looping `<video>` element with `autoPlay muted loop playsInline` (all four attributes required for browser autoplay)
- Dark overlay (`bg-black/50`) on top of video for text readability
- For the video source, use a free stock video URL from Pexels or similar. If a local video is not available, use a dark gradient background as fallback with a comment noting where to place the video file (`/public/videos/hero.mp4`)
- Fallback approach: Use a rich dark gradient (`bg-gradient-to-br from-slate-900 via-indigo-950 to-slate-900`) as the background if no video file exists, with the video element overlaid on top. This ensures the hero always looks good even without the video file.

**Content (centered, over video):**
- Small text above headline: "Introducing Anvara" or similar (from screenshot, light gray)
- Main headline: "Connect Your Brand to Culture at Scale" — large, white, bold, serif or sans-serif matching Anvara's look
- Subtext below headline: "Anvara gives brands effortless access to high-impact sponsorships. Instantly discover opportunities, compare options, close deals, and measure success—all in one platform." — white/70 opacity
- Single CTA button: "Try Anvara Free" — blue (`bg-primary`), links to `/browse`, centered, with arrow icon
- Per CONTEXT.md: "Try Anvara Free" routes to /browse (no mock login page)

**Property Card Carousel (bottom of hero):**
- Use Embla carousel (already installed) with Autoplay plugin
- `useRef` pattern for Autoplay (per project decision 04-02): `const plugin = useRef(Autoplay({ delay: 3000, stopOnMouseEnter: true, stopOnInteraction: false }))`
- Shows all 12 seeded properties using LandingPropertyCard
- `basis-1/3` on lg, `basis-1/2` on md, `basis-[85%]` on mobile for responsive
- `loop: true`, `align: "start"` in Embla opts
- Cards sit at the bottom of the hero with slight overlap or positioned at the bottom of the viewport

**Component signature:**
```tsx
interface HeroSectionProps {
  properties: Array<{
    id: string;
    name: string;
    category: string;
    subcategory: string | null;
    region: string;
    heroImage: string;
  }>;
}
export function HeroSection({ properties }: HeroSectionProps)
```

The section should be `relative min-h-screen` to fill the viewport. Content vertically centered. Carousel at the bottom (~20% from bottom).
  </action>
  <verify>
`npx tsc --noEmit` passes. Component exports HeroSection. Verify carousel code follows Embla v8 patterns.
  </verify>
  <done>
Hero section renders video/gradient background, headline "Connect Your Brand to Culture at Scale", "Try Anvara Free" CTA linking to /browse, and a snap carousel of all 12 property cards auto-advancing every 3s.
  </done>
</task>

<task type="auto">
  <name>Task 2: Brand Logos Bar, What is Anvara, and Access the Whole Market sections</name>
  <files>
    app/_components/landing/brand-logos-bar.tsx
    app/_components/landing/what-is-anvara.tsx
    app/_components/landing/access-market.tsx
  </files>
  <action>
Create three section components. Reference screenshots Landing2.png and Landing3.png.

**1. Brand Logos Bar (`brand-logos-bar.tsx`):**
- Simple horizontal row of brand logos: micro1, Legendz, Delta, gopuff (matching Anvara exactly per CONTEXT.md)
- Small "USED BY TEAMS AT" or "LOVED BY TEAMS AT" label above logos (from screenshot)
- Logos rendered as text-based representations or inline SVG. For MVP, use styled text with brand fonts where possible: "micro1." in serif italic, "LEGENDZ" in bold caps, triangle + "DELTA" for Delta, "gopuff" in brand style
- Grayscale treatment: `opacity-60` on the container
- Light background (`bg-muted/50` or similar), centered, `py-8` padding
- Wrap in SectionReveal for scroll animation
- Server Component (no client interactivity needed)

**2. What is Anvara (`what-is-anvara.tsx`):**
- "use client" component (needs useState + useEffect for rotating text)
- Small badge at top: "What is Anvara?"
- Main text: "Anvara is the marketplace for [rotating word] sponsorships"
- Rotating words cycle: "Music Event", "Food Festival", "Action Sports", "Music Festival", "Tech Conference" — changes every 2.5 seconds
- Use `useState` + `setInterval` for word cycling (per research Pattern 6)
- The rotating word should be in `text-primary` color to stand out
- Add smooth opacity transition when word changes (fade out old, fade in new)
- Below text: "We help brands and rightsholders bring marketing to life." subtext
- 3D token illustrations: Create 3 abstract token/coin shapes using CSS. From screenshot, they look like rounded white pill shapes with subtle shadows and icons inside (music note, sports, food). Approximate with `rounded-2xl bg-white shadow-xl` divs with slight 3D rotation via `transform: perspective(800px) rotateY(-15deg)` and lucide icons inside
- Center-aligned section with generous padding

**3. Access the Whole Market (`access-market.tsx`):**
- Left side: heading "Access the Whole Market" (large, bold) + body text explaining Anvara's market access value prop: "Anvara brings the biggest brands and rightsholders to one unified platform. With full visibility, more options, and direct contact, deals get done smarter, faster, and with better-to-possible alignment."
- Two CTA buttons above the property list: "Try it Free" (blue/primary) and "Contact Us" (outline) — both link to /browse
- Right side: A list of 4-5 property names from seeded data with their logos/icons, styled as a card list. Each shows a small thumbnail, property name, and a small metric badge (e.g., "4.4k views today" like in the screenshot). Pass properties as a prop.
- Below: "Check the Market" CTA button (blue, links to /browse)
- Use SectionReveal wrapper
- Takes properties array as prop for the property list

Component signature:
```tsx
interface AccessMarketProps {
  properties: Array<{ id: string; name: string; heroImage: string; subcategory: string | null; category: string }>;
}
```
  </action>
  <verify>
`npx tsc --noEmit` passes. All three components export their named exports. Rotating text uses setInterval with cleanup.
  </verify>
  <done>
BrandLogosBar shows 4 brand logos in grayscale. WhatIsAnvara cycles through event types with animated text and shows 3D token illustrations. AccessMarket displays a property list with "Check the Market" CTA linking to /browse.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All four component files exist under `app/_components/landing/`
3. HeroSection uses Embla carousel with Autoplay v8 (delay: 3000, stopOnMouseEnter, stopOnInteraction: false)
4. All CTAs link to `/browse`
5. WhatIsAnvara has proper setInterval cleanup in useEffect
6. LandingPropertyCard is imported and used in hero carousel
</verification>

<success_criteria>
- Hero, Brand Logos, What is Anvara, and Access Market components compile and export correctly
- Hero has video/gradient background, headline, CTA, and auto-scrolling carousel
- Rotating text cycles through event types
- All CTAs route to /browse
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-02-SUMMARY.md`
</output>
