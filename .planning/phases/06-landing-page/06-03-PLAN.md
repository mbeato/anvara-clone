---
phase: 06-landing-page
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/_components/landing/az-execution.tsx
  - app/_components/landing/performance-reporting.tsx
  - app/_components/landing/anvara-intelligence.tsx
autonomous: true

must_haves:
  truths:
    - "A-Z Execution section shows a Before/After UI mockup with deal flow cards and a 'Make a Deal' CTA"
    - "Performance Reporting section shows metrics visualization (125.2M Impressions, 144 Mentions, 4.0x ROI) with colored badges and curved lines"
    - "Anvara Intelligence section shows an AI matching flow diagram with prompt form and property results"
    - "All three sections use SectionReveal for scroll-triggered animations"
    - "All CTAs link to /browse"
  artifacts:
    - path: "app/_components/landing/az-execution.tsx"
      provides: "Before/After deal flow mockup with CTA"
      exports: ["AZExecution"]
    - path: "app/_components/landing/performance-reporting.tsx"
      provides: "Metrics visualization with colored badges"
      exports: ["PerformanceReporting"]
    - path: "app/_components/landing/anvara-intelligence.tsx"
      provides: "AI matching flow diagram with prompt form UI"
      exports: ["AnvaraIntelligence"]
  key_links:
    - from: "app/_components/landing/az-execution.tsx"
      to: "app/_components/landing/section-reveal.tsx"
      via: "SectionReveal wrapper"
      pattern: "SectionReveal"
    - from: "app/_components/landing/performance-reporting.tsx"
      to: "app/_components/landing/section-reveal.tsx"
      via: "SectionReveal wrapper"
      pattern: "SectionReveal"
    - from: "app/_components/landing/anvara-intelligence.tsx"
      to: "app/_components/landing/section-reveal.tsx"
      via: "SectionReveal wrapper"
      pattern: "SectionReveal"
---

<objective>
Build the three value proposition sections — A-Z Execution (Before/After mockup), Performance Reporting (metrics visualization), and Anvara Intelligence (AI matching flow diagram).

Purpose: These sections demonstrate Anvara's core product capabilities through custom-built UI mockup illustrations. They are NOT screenshots — they are actual styled UI components that recreate the visual mockups from the Anvara website, showing execution quality.

Output: Three section components with custom UI illustrations, ready for page assembly.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@app/_components/landing/section-reveal.tsx

Reference screenshots:
- screenshots/Landing4.png — A-Z Execution section (Before/After deal flow, "Make a Deal" CTA)
- screenshots/Landing5.png — Performance Reporting section (lollapalooza card with metric bubbles and curved lines)
- screenshots/Landing6.png — Anvara Intelligence section (prompt form, AI flow, property results)
- screenshots/Landing7.png — Bottom of Intelligence section showing completion state
</context>

<tasks>

<task type="auto">
  <name>Task 1: A-Z Execution section with Before/After mockup</name>
  <files>
    app/_components/landing/az-execution.tsx
  </files>
  <action>
Create `app/_components/landing/az-execution.tsx`. Reference screenshot Landing4.png.

**Layout:** Two-column on desktop — left side has the Before/After mockup illustration, right side has the text content. Stacks on mobile.

**Right side text content:**
- Small badge/label: "A-Z Execution"
- Heading: "No more endless decks & emails. A-Z execution in one place."
- Body: "Anvara is the operating system for sponsorship deals—start to finish. Send offers, sign contracts, pay, share creative, and track performance in one streamlined flow. Bring deals to life faster, with less friction."
- CTA button: "Make a Deal" (blue/primary, links to /browse, with arrow icon →)

**Left side — Before/After UI mockup (build as actual JSX, NOT an image):**
Per CONTEXT.md: "Recreate the UI mockup illustrations with custom styling — not screenshots"

From the screenshot, the mockup shows:
- A card-like container with rounded corners and light shadow
- "Before" label (left/top) and "After" label (right/top) as rounded pill badges
- Below: A vertical list of 3-4 deal items, each showing:
  - A small colored avatar/icon (red triangle for "Los Angeles", purple for "Lakers", red circle for "Disney", green for "PGA Tour")
  - Property name and a status like "Sponsor Deck Attached" or "210 pages"
  - Some items have colored status badges: blue "New", green checkmarks
- Center has a cluster of small action icons (chat, document, calendar, check) in a floating pill
- The "After" side shows a cleaner, simplified version

Build this as:
- A `relative` container with `bg-white rounded-2xl shadow-lg p-6`
- Before/After toggle pills at top: `absolute top-4 left-4` and `top-4 right-4`, styled as `px-3 py-1 rounded-full bg-muted text-sm font-medium`
- 3-4 mock deal items as flexbox rows: small colored circle (avatar), text column (name + subtitle), optional status badge
- Small floating action bar in the center: a `rounded-full bg-slate-800 text-white` pill with 4-5 small lucide icons (MessageSquare, FileText, Calendar, Check, BarChart)
- Use subtle shadows and borders to match the clean mockup aesthetic

Wrap the entire section in SectionReveal.
  </action>
  <verify>
`npx tsc --noEmit` passes. Component exports AZExecution. Contains "Make a Deal" CTA linking to /browse.
  </verify>
  <done>
A-Z Execution section renders a Before/After deal flow mockup as actual UI components (not images) with heading, body text, and "Make a Deal" CTA.
  </done>
</task>

<task type="auto">
  <name>Task 2: Performance Reporting and Anvara Intelligence sections</name>
  <files>
    app/_components/landing/performance-reporting.tsx
    app/_components/landing/anvara-intelligence.tsx
  </files>
  <action>
**1. Performance Reporting (`performance-reporting.tsx`):**
Reference screenshot Landing5.png.

Layout: Two-column — left side text, right side metrics visualization.

Left side text:
- Badge: "Analytics"
- Heading: "Built-In Performance Reporting"
- Body: "Measure success without extra tools. Track engagement, reach, and ROI on every campaign—so you get the credit and insights you deserve."
- CTA: "Measure Success" (blue/primary, links to /browse, arrow →)

Right side — Metrics visualization mockup (build as JSX, NOT image):
From the screenshot (Landing5.png), this shows:
- A large rounded card (`bg-muted/50 rounded-2xl p-8`) as the container
- Inside: A smaller white card with property info (Lollapalooza 2025, Chicago, IL) with a small gradient avatar
- Three metric badge pills connected by curved SVG lines:
  - Blue: "◎ 125.2M Impressions" — positioned top-right
  - Red: "◎ 144 Mentions" — positioned center-right
  - Green/olive: "◎ 4.0x ROI" — positioned bottom-left
- Curved SVG `<path>` elements connecting the property card to each badge using quadratic bezier curves
- Use absolute positioning within a `relative` container

Build the metric badges as:
```
<span className="absolute [position] px-3 py-1.5 rounded-full text-white text-xs font-semibold bg-blue-500">
  ◎ 125.2M Impressions
</span>
```

Build the curved lines as an SVG overlay:
```
<svg className="absolute inset-0 w-full h-full" style={{ overflow: 'visible' }}>
  <path d="M ... C ..." stroke="#3B82F6" strokeWidth="2" fill="none" strokeLinecap="round" />
</svg>
```

Use 3 quadratic bezier paths from the card area to each badge position. Colors: blue (#3B82F6), red (#EF4444), green/olive (#84CC16).

Wrap in SectionReveal.

**2. Anvara Intelligence (`anvara-intelligence.tsx`):**
Reference screenshots Landing6.png and Landing7.png.

Layout: Text at top (centered), flow diagram below.

Top text:
- Badge: "Sponsorship Meets AI"
- Heading: "Make smarter, faster marketing decisions with **Anvara Intelligence**" (the "Anvara Intelligence" part in primary color)
- Body: "Describe your brand, target audience, and goals. Our AI scans thousands of listings to match you with the most relevant partnerships—instantly."
- CTA: "Try Anvara Intelligence" (blue/primary, centered, links to /browse, arrow →)

Below — AI matching flow diagram (build as JSX):
From screenshots Landing6.png and Landing7.png, this shows a left-to-right flow:

Left panel — Prompt form card:
- White card with shadow, containing form fields:
  - "Prompt" label + textarea with example: "I want to run a campaign targeting college students on the West Coast for our new delivery promotion"
  - "Brand" dropdown showing "Gopuff"
  - "Region" dropdown showing "California" with flag emoji
  - "Age Demographic" field
  - "Budget" dropdown showing "$100K"

Center — Flow arrows:
- Dotted horizontal arrow lines pointing right with small arrow heads
- "Anvara Intelligence" label with Anvara logo icon in the center
- "Completed!" badge in green below

Right panel — Results list:
- 4-5 property result cards stacked vertically:
  - "LAFC Sponsorships" with small avatar
  - "UCLA Bruins Sponsorships" with avatar
  - "USC Football Sponsorships" with avatar
  - "Outside Lands Music Festival" with avatar
- Each is a rounded card with border

Build the flow diagram as:
- 3-column grid or flex layout
- Left: form card mockup (not functional, just visual)
- Center: arrow + Anvara logo + completed badge
- Right: results list

Use SectionReveal on the outer container and optionally stagger the inner elements with delay props.
  </action>
  <verify>
`npx tsc --noEmit` passes. Both components export correctly. SVG paths render without errors. All CTAs link to /browse.
  </verify>
  <done>
PerformanceReporting renders metrics visualization with three colored metric badges (125.2M Impressions, 144 Mentions, 4.0x ROI) connected by curved SVG lines. AnvaraIntelligence renders AI matching flow diagram with prompt form, flow arrows, and property results list. Both include their respective CTAs.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Three component files exist under `app/_components/landing/`
3. All three use SectionReveal for scroll animation
4. All CTAs link to `/browse`
5. UI mockups are built as actual JSX components (not images)
6. Metrics visualization has SVG curved lines connecting badges
7. AI flow diagram has left-center-right layout matching screenshots
</verification>

<success_criteria>
- A-Z Execution Before/After mockup renders as JSX components
- Metrics visualization shows 125.2M Impressions, 144 Mentions, 4.0x ROI badges with curved SVG lines
- AI Intelligence flow diagram shows prompt form → Anvara → results
- All sections use SectionReveal for scroll animations
- All CTAs route to /browse
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-03-SUMMARY.md`
</output>
