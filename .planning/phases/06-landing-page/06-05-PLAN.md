---
phase: 06-landing-page
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Landing page at / renders all 12 sections in the correct order from top to bottom"
    - "Hero section fills the viewport with video/gradient background, headline, and scrolling property cards"
    - "All scroll-triggered section reveals animate smoothly on viewport entry"
    - "All CTAs throughout the page navigate to /browse"
    - "FAQ accordion works (expand/collapse)"
    - "Marquee carousel in Final CTA scrolls continuously"
    - "Page renders outside the app shell (no sidebar)"
    - "All existing routes (/browse, /messages, /properties/*) still work with sidebar"
  artifacts:
    - path: "app/page.tsx"
      provides: "Complete landing page assembling all 12 sections"
      min_lines: 40
  key_links:
    - from: "app/page.tsx"
      to: "lib/data/index.ts"
      via: "getAllProperties() for property data"
      pattern: "getAllProperties"
    - from: "app/page.tsx"
      to: "app/_components/landing/hero-section.tsx"
      via: "HeroSection import"
      pattern: "HeroSection"
    - from: "app/page.tsx"
      to: "app/_components/landing/landing-footer.tsx"
      via: "LandingFooter import"
      pattern: "LandingFooter"
    - from: "app/page.tsx"
      to: "app/_components/landing/access-market.tsx"
      via: "AccessMarket properties prop"
      pattern: "AccessMarket"
    - from: "app/page.tsx"
      to: "app/_components/landing/final-cta.tsx"
      via: "FinalCTA properties prop"
      pattern: "FinalCTA"
---

<objective>
Assemble all 12 landing page sections into `app/page.tsx` and verify the complete page works end-to-end.

Purpose: This is the final integration — all section components from plans 02-04 are imported and rendered in the correct order. The result is a complete, polished landing page that faithfully reproduces Anvara's current site.

Output: Complete landing page at `/` with all 12 sections rendering correctly.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@app/page.tsx
@app/_components/landing/hero-section.tsx
@app/_components/landing/brand-logos-bar.tsx
@app/_components/landing/what-is-anvara.tsx
@app/_components/landing/access-market.tsx
@app/_components/landing/az-execution.tsx
@app/_components/landing/performance-reporting.tsx
@app/_components/landing/anvara-intelligence.tsx
@app/_components/landing/for-brands-rightsholders.tsx
@app/_components/landing/testimonial.tsx
@app/_components/landing/faq-section.tsx
@app/_components/landing/final-cta.tsx
@app/_components/landing/landing-footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble all sections into page.tsx</name>
  <files>
    app/page.tsx
  </files>
  <action>
Update `app/page.tsx` (created in 06-01 as placeholder) to import and render all 12 landing page sections in the correct order.

The page is a Server Component — it fetches properties once and passes them down to sections that need them.

```tsx
import { getAllProperties } from "@/lib/data";
import { HeroSection } from "./_components/landing/hero-section";
import { BrandLogosBar } from "./_components/landing/brand-logos-bar";
import { WhatIsAnvara } from "./_components/landing/what-is-anvara";
import { AccessMarket } from "./_components/landing/access-market";
import { AZExecution } from "./_components/landing/az-execution";
import { PerformanceReporting } from "./_components/landing/performance-reporting";
import { AnvaraIntelligence } from "./_components/landing/anvara-intelligence";
import { ForBrandsRightsholders } from "./_components/landing/for-brands-rightsholders";
import { Testimonial } from "./_components/landing/testimonial";
import { FaqSection } from "./_components/landing/faq-section";
import { FinalCTA } from "./_components/landing/final-cta";
import { LandingFooter } from "./_components/landing/landing-footer";

export default async function LandingPage() {
  const properties = await getAllProperties();

  return (
    <main className="min-h-screen bg-background overflow-x-hidden">
      {/* Section 1: Hero — Video bg, headline, CTA, snap carousel */}
      <HeroSection properties={properties} />

      {/* Section 2: Brand logos bar */}
      <BrandLogosBar />

      {/* Section 3: What is Anvara — rotating text, 3D tokens */}
      <WhatIsAnvara />

      {/* Section 4: Access the Whole Market — property list, CTA */}
      <AccessMarket properties={properties} />

      {/* Section 5: A-Z Execution — Before/After mockup */}
      <AZExecution />

      {/* Section 6: Performance Reporting — metrics visualization */}
      <PerformanceReporting />

      {/* Section 7: Anvara Intelligence — AI flow diagram */}
      <AnvaraIntelligence />

      {/* Section 8: For Brands / For Rightsholders — logo grids */}
      <ForBrandsRightsholders />

      {/* Section 9: Testimonial — GoPuff quote */}
      <Testimonial />

      {/* Section 10: FAQ — 6 accordion items */}
      <FaqSection />

      {/* Section 11: Final CTA — blue banner + marquee */}
      <FinalCTA properties={properties} />

      {/* Section 12: Footer */}
      <LandingFooter />
    </main>
  );
}
```

Section order matches CONTEXT.md exactly (12 sections, top to bottom).

Properties are passed to:
- HeroSection (snap carousel)
- AccessMarket (property list)
- FinalCTA (marquee carousel)

Other sections are self-contained (no data props needed).

Add `overflow-x-hidden` to prevent horizontal scroll from marquee animation.

If any section component has a type mismatch with the properties type, add a type annotation or adjust the property selection. The `getAllProperties()` return includes all fields (id, name, category, subcategory, region, heroImage, etc.) so it should satisfy all section prop interfaces.

Fix any TypeScript errors that arise from the integration. If a component has minor issues (wrong prop name, missing export), fix it in this task.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Run `npm run dev` and visit `http://localhost:3000/` — all 12 sections should render in order. Check that `/browse` still loads with the sidebar.
  </verify>
  <done>
All 12 sections render on the landing page in the correct order. Page compiles without errors. Properties data flows from server to client sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Anvara landing page with all 12 sections: Hero (video/gradient bg + headline + CTA + snap carousel), Brand Logos, What is Anvara (rotating text + 3D tokens), Access the Whole Market (property list), A-Z Execution (Before/After mockup), Performance Reporting (metrics viz), Anvara Intelligence (AI flow diagram), For Brands/Rightsholders (logo grids), Testimonial (GoPuff quote), FAQ (6 accordion items), Final CTA (blue banner + marquee), Footer (links + newsletter + watermark).</what-built>
  <how-to-verify>
1. Open http://localhost:3000/ in your browser
2. Verify the landing page loads WITHOUT the sidebar (this is the public-facing page)
3. Scroll through all 12 sections — each should animate in on scroll (fade up)
4. Check Hero: Does the headline, CTA, and property card carousel look right? Do cards auto-advance?
5. Check rotating text in "What is Anvara" — do the words cycle?
6. Check the UI mockup sections (A-Z Execution, Performance Reporting, Intelligence) — do they look like actual product mockups?
7. Click "Try Anvara Free" or any CTA — should navigate to /browse WITH the sidebar
8. Check FAQ — click items, only one should open at a time
9. Check Final CTA — blue banner with smooth scrolling property cards
10. Check Footer — "Prototype by Max Beato" attribution present, newsletter input is visual
11. Open http://localhost:3000/browse — verify sidebar and browse page still work
12. Open http://localhost:3000/messages — verify messaging still works
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 6, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run dev` serves the complete landing page at `/`
3. All 12 sections render in correct order
4. Scroll animations work (sections fade in on viewport entry)
5. Hero carousel auto-advances every 3 seconds
6. Rotating text cycles through event types
7. FAQ accordion allows one item open at a time
8. Marquee in Final CTA scrolls continuously
9. All CTAs navigate to /browse
10. Existing routes (/browse, /messages, /properties/*) unaffected
11. Footer shows "Prototype by Max Beato"
12. No console errors
</verification>

<success_criteria>
- Complete landing page with all 12 sections renders at /
- Page is outside the app shell (no sidebar)
- All animations are smooth
- All CTAs route to /browse
- All existing app functionality preserved
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-05-SUMMARY.md`
</output>
