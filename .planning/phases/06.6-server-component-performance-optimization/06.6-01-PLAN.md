---
phase: 06.6-server-component-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/deals/_components/deals-pipeline.tsx
  - app/(app)/campaigns/_components/campaigns-table.tsx
  - app/(app)/listings/_components/empty-state.tsx
  - app/(app)/listings/_components/active-filter-chips.tsx
autonomous: true

must_haves:
  truths:
    - "No page-level useEffect for data fetching — all data loads in Server Components via async/await"
    - "Client components ('use client') are only on interactive leaf components (forms, modals, buttons), never on display-only components that render props without hooks"
    - "Build passes cleanly (npm run build) with no TypeScript errors"
  artifacts:
    - path: "app/(app)/deals/_components/deals-pipeline.tsx"
      provides: "Server Component deals pipeline display"
      contains_not: '"use client"'
    - path: "app/(app)/campaigns/_components/campaigns-table.tsx"
      provides: "Server Component campaigns table display"
      contains_not: '"use client"'
    - path: "app/(app)/listings/_components/empty-state.tsx"
      provides: "Server Component empty state display"
      contains_not: '"use client"'
    - path: "app/(app)/listings/_components/active-filter-chips.tsx"
      provides: "Server Component active filter chips display"
      contains_not: '"use client"'
  key_links:
    - from: "app/(app)/deals/page.tsx"
      to: "app/(app)/deals/_components/deals-pipeline.tsx"
      via: "Server Component import — no client boundary needed"
    - from: "app/(app)/campaigns/page.tsx"
      to: "app/(app)/campaigns/_components/campaigns-table.tsx"
      via: "Server Component import — no client boundary needed"
    - from: "app/(app)/listings/_components/browse-client.tsx"
      to: "app/(app)/listings/_components/empty-state.tsx"
      via: "Client Component renders Server Component child via props — onClick passed down, no hooks in empty-state"
    - from: "app/(app)/listings/_components/browse-client.tsx"
      to: "app/(app)/listings/_components/active-filter-chips.tsx"
      via: "Client Component renders Server Component child via props — onClick handlers passed as props"
---

<objective>
Remove unnecessary `"use client"` directives from four display-only components that have no hooks, no state, and no browser API usage. These components are pure renderers of props and should be Server Components, reducing client bundle size and correctly signaling to Next.js that they require no client JavaScript.

Purpose: Enforce the Server Component performance pattern — `"use client"` only on components that genuinely use hooks or browser APIs, never on display-only components.
Output: Four files with `"use client"` removed, clean build confirmation.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-server-component-performance-optimization/06.6-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "use client" from four display-only components</name>
  <files>
    app/(app)/deals/_components/deals-pipeline.tsx
    app/(app)/campaigns/_components/campaigns-table.tsx
    app/(app)/listings/_components/empty-state.tsx
    app/(app)/listings/_components/active-filter-chips.tsx
  </files>
  <action>
    Remove the `"use client"` directive (the first line) from each of these four files. These are pure display components that render props without any hooks, state, or browser APIs:

    1. **`deals-pipeline.tsx`** — Pure JSX render of `deals` prop into a stage-grouped grid. Uses `Badge`, `Card` from ShadCN (those have their own client boundaries) but the pipeline component itself has zero hooks. Remove line 1 (`"use client"`).

    2. **`campaigns-table.tsx`** — Pure JSX render of `campaigns` prop into an HTML table. Uses `Badge`, `Card` from ShadCN but the table component itself has zero hooks. Remove line 1 (`"use client"`).

    3. **`empty-state.tsx`** — Renders an icon, message text, and a Button with `onClearFilters` onClick prop passed in from parent. The `onClick` is a prop — it does not require `"use client"` on this component. The parent (`browse-client.tsx`) is already a client component and passes the handler down. Remove line 1 (`"use client"`).

    4. **`active-filter-chips.tsx`** — Renders filter badges with `onClick` removal handlers passed as props. No hooks, no state. The `onClick` handlers are callbacks from the parent client component. Remove line 1 (`"use client"`).

    For each file: delete the `"use client"` line and the blank line after it (if any), so the file starts with the first `import` statement.

    Do NOT modify any other lines in these files.
  </action>
  <verify>
    Run `grep -rn '"use client"' app/(app)/deals/_components/deals-pipeline.tsx app/(app)/campaigns/_components/campaigns-table.tsx app/(app)/listings/_components/empty-state.tsx app/(app)/listings/_components/active-filter-chips.tsx` — should return zero results.
  </verify>
  <done>All four files have no `"use client"` directive. The first line of each file is an `import` statement.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification</name>
  <files>(no files modified — verification only)</files>
  <action>
    Run `npm run build` to confirm:
    1. No TypeScript errors introduced by removing `"use client"`
    2. No runtime errors from Server Component rendering
    3. Build completes successfully with exit code 0

    If the build fails with a hook-related error (e.g., "Hooks can only be called inside a function component"), it means the component actually uses hooks and `"use client"` should be restored. Check the error message, restore the directive for the offending file, and re-run the build.

    Expected: Clean build. The research audit confirmed these four components have zero hooks.
  </action>
  <verify>
    `npm run build` exits with code 0 and shows "Compiled successfully" or equivalent success output.
  </verify>
  <done>Build passes with no TypeScript errors. All routes compile. The four components render as Server Components without issues.</done>
</task>

</tasks>

<verification>
1. `grep -rn '"use client"' app/(app)/deals/_components/deals-pipeline.tsx app/(app)/campaigns/_components/campaigns-table.tsx app/(app)/listings/_components/empty-state.tsx app/(app)/listings/_components/active-filter-chips.tsx` returns empty (zero matches)
2. `npm run build` completes with exit code 0
3. No new TypeScript errors in the build output
</verification>

<success_criteria>
- Four display-only components no longer have `"use client"` directives
- Build passes cleanly with no TypeScript errors
- All pages that use these components (deals, campaigns, listings) compile and route correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06.6-server-component-performance-optimization/06.6-01-SUMMARY.md`
</output>
