---
phase: 04-browse-and-discovery
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/(app)/browse/_components/category-carousel.tsx
  - app/(app)/browse/_components/category-tab-row.tsx
  - app/(app)/browse/_components/browse-client.tsx
  - app/(app)/browse/page.tsx
autonomous: true

must_haves:
  truths:
    - "Auto-scrolling category carousel displays at the top of the browse page with image cards"
    - "Carousel auto-scrolls and pauses on hover"
    - "Compact text-only category tab row appears below the carousel"
    - "Clicking a category tab filters the card grid to that category via URL param"
    - "Category tab row shows All plus the 5 seeded categories"
  artifacts:
    - path: "app/(app)/browse/_components/category-carousel.tsx"
      provides: "Auto-scrolling Embla carousel with category hero images"
      exports: ["CategoryCarousel"]
    - path: "app/(app)/browse/_components/category-tab-row.tsx"
      provides: "Compact text-only category navigation row"
      exports: ["CategoryTabRow"]
    - path: "app/(app)/browse/_components/browse-client.tsx"
      provides: "Client Component shell orchestrating carousel, tabs, filter bar, grid, and recommendations"
      exports: ["BrowseClient"]
  key_links:
    - from: "app/(app)/browse/_components/browse-client.tsx"
      to: "next/navigation"
      via: "useRouter + usePathname for URL filter updates"
      pattern: "router\\.replace"
    - from: "app/(app)/browse/_components/category-carousel.tsx"
      to: "embla-carousel-autoplay"
      via: "Autoplay plugin passed to Carousel plugins prop"
      pattern: "Autoplay\\("
---

<objective>
Build the auto-scrolling category image carousel with Embla autoplay, the compact text-only category tab row, and the BrowseClient shell component that orchestrates all browse page UI.

Purpose: The carousel and tab row are the primary discovery elements — they give advertisers a visual, engaging way to explore sponsorship categories. The BrowseClient wires filter state management via URL params.
Output: Category carousel with autoplay, clickable tab row that filters properties, and a client component shell ready for filter bar and recommendations integration.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-browse-and-discovery/04-CONTEXT.md
@.planning/phases/04-browse-and-discovery/04-RESEARCH.md
@.planning/phases/04-browse-and-discovery/04-01-SUMMARY.md
@components/ui/carousel.tsx
@components/ui/badge.tsx
@app/(app)/browse/page.tsx
@app/(app)/browse/_components/property-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryCarousel with Embla autoplay</name>
  <files>app/(app)/browse/_components/category-carousel.tsx</files>
  <action>
Create `app/(app)/browse/_components/category-carousel.tsx` as a "use client" component.

**Category data:** Define a `CATEGORIES` constant array with objects: `{ slug, label, imageUrl }`. Include these categories matching seed data plus aspirational ones for visual richness:
- `{ slug: "sports", label: "Action Sports", imageUrl: "https://picsum.photos/seed/sports-arena/600/300" }`
- `{ slug: "music", label: "Music Festivals", imageUrl: "https://picsum.photos/seed/music-festival/600/300" }`
- `{ slug: "arts", label: "Art & Culture", imageUrl: "https://picsum.photos/seed/art-gallery/600/300" }`
- `{ slug: "food", label: "Food & Culinary", imageUrl: "https://picsum.photos/seed/food-festival/600/300" }`
- `{ slug: "lifestyle", label: "Lifestyle Events", imageUrl: "https://picsum.photos/seed/lifestyle-outdoor/600/300" }`
- `{ slug: "tech", label: "Tech Conferences", imageUrl: "https://picsum.photos/seed/tech-conference/600/300" }`
- `{ slug: "charity", label: "Charity Galas", imageUrl: "https://picsum.photos/seed/charity-gala/600/300" }`
- `{ slug: "fashion", label: "Fashion Week", imageUrl: "https://picsum.photos/seed/fashion-runway/600/300" }`

**Implementation:**
1. Import `Autoplay` from `embla-carousel-autoplay`
2. Use `useRef` to store the Autoplay plugin instance:
   ```typescript
   const plugin = useRef(
     Autoplay({ delay: 3000, stopOnMouseEnter: true, stopOnInteraction: false })
   )
   ```
3. Render using ShadCN `<Carousel>` with `plugins={[plugin.current]}` and `opts={{ loop: true, align: "start" }}`
4. Each `<CarouselItem>` has `className="basis-1/2 md:basis-1/3 lg:basis-1/5 pl-4"` — shows 5 per viewport on desktop
5. Each carousel card:
   - Rounded container with `relative aspect-[2/1] overflow-hidden rounded-xl cursor-pointer group`
   - `<Image>` with fill, object-cover, `sizes="(max-width: 768px) 50vw, (max-width: 1024px) 33vw, 20vw)"`
   - Dark gradient overlay at bottom: `absolute inset-0 bg-gradient-to-t from-black/60 to-transparent`
   - Category label at bottom: `absolute bottom-3 left-3 text-white font-semibold text-sm`
   - On hover: slight scale up `group-hover:scale-105 transition-transform duration-300` on the image

6. Accept an `onCategoryClick` prop `(slug: string) => void` — clicking a carousel card triggers the category filter
7. Do NOT render dot indicators or arrow buttons — keep it clean per user's "feel less cluttered" goal. Only the auto-scroll loop.

**NO left/right navigation arrows on this carousel.** The user specifically wants less clutter than the current Anvara carousel.
  </action>
  <verify>
```bash
npx tsc --noEmit 2>&1 | head -20
```
Component compiles. Autoplay import resolves.
  </verify>
  <done>Category carousel auto-scrolls with 3s delay, pauses on hover, shows 5 category cards per row on desktop with gradient overlay labels, and triggers category filter on click.</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryTabRow and BrowseClient shell</name>
  <files>app/(app)/browse/_components/category-tab-row.tsx, app/(app)/browse/_components/browse-client.tsx, app/(app)/browse/page.tsx</files>
  <action>
**Create `app/(app)/browse/_components/category-tab-row.tsx`** — "use client" component:

1. Define tab categories matching the carousel CATEGORIES plus an "All" option at the start:
   - `{ slug: undefined, label: "All" }` (undefined slug = no category filter)
   - `{ slug: "sports", label: "Sports" }`
   - `{ slug: "music", label: "Music" }`
   - `{ slug: "arts", label: "Arts" }`
   - `{ slug: "food", label: "Food" }`
   - `{ slug: "lifestyle", label: "Lifestyle" }`

2. Text-only category labels (no icons — per user decision: "Compact text-only category label row below carousel")
3. Props: `activeCategory: string | undefined`, `onCategoryChange: (slug: string | undefined) => void`
4. Render as a horizontal scrollable row: `flex gap-2 overflow-x-auto pb-2 scrollbar-hide`
5. Each tab is a `<button>` with:
   - Base: `px-4 py-1.5 rounded-full text-sm font-medium whitespace-nowrap transition-colors`
   - Active state: `bg-primary text-primary-foreground` (Anvara brand blue)
   - Inactive state: `bg-muted text-muted-foreground hover:bg-accent`
6. Clicking a tab calls `onCategoryChange(slug)` — the parent (BrowseClient) handles URL update

**Create `app/(app)/browse/_components/browse-client.tsx`** — "use client" main orchestrator:

1. Props:
   ```typescript
   interface BrowseClientProps {
     properties: Property[]  // Use the same type as PropertyCard
     currentFilters: {
       category?: string
       region?: string
       minPrice?: number
       maxPrice?: number
     }
     hasFilters: boolean
   }
   ```
2. Import `useRouter`, `usePathname` from `next/navigation`
3. Implement `updateFilter(key: string, value: string | undefined)`:
   - Build URLSearchParams from `currentFilters`, update the changed key
   - If value is undefined/empty, omit that key from params
   - Call `router.replace(pathname + (qs ? "?" + qs : ""), { scroll: false })`
4. Implement `clearFilters()`: `router.replace(pathname, { scroll: false })`
5. Layout structure (top to bottom):
   - `<CategoryCarousel onCategoryClick={(slug) => updateFilter("category", slug)} />`
   - `<CategoryTabRow activeCategory={currentFilters.category} onCategoryChange={(slug) => updateFilter("category", slug)} />`
   - Placeholder div for filter bar: `{/* FilterBar — wired in Plan 04-03 */}`
   - Placeholder div for recommendations: `{/* RecommendationsStrip — wired in Plan 04-04 */}`
   - Property count label: `<p className="text-sm text-muted-foreground">{properties.length} properties</p>`
   - Property grid: `<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4">` rendering PropertyCard for each property
   - If `properties.length === 0`: render inline empty state placeholder (will be replaced by EmptyState component in 04-03)
6. Wrap the entire page in `<div className="max-w-7xl mx-auto px-4 py-6 space-y-6">`

**Update `app/(app)/browse/page.tsx`:**

Replace the temporary inline layout from Plan 04-01 with:
```typescript
import { BrowseClient } from "./_components/browse-client"
// ...existing searchParams parsing...
return <BrowseClient properties={properties} currentFilters={{ category, region, minPrice, maxPrice }} hasFilters={hasFilters} />
```

The page.tsx remains a Server Component — it fetches data and passes to the client shell.
  </action>
  <verify>
```bash
npx tsc --noEmit 2>&1 | head -20
```
All three files compile. Clicking category tabs updates URL params. Carousel renders with autoplay.
  </verify>
  <done>
- BrowseClient orchestrates the browse page layout with URL-based filter state
- CategoryTabRow shows "All" + 5 category tabs; clicking filters the grid via URL
- CategoryCarousel cards also trigger the category filter on click
- Category tab and carousel are in sync (both use the same `category` URL param)
- Property grid renders below with 5 columns on desktop
  </done>
</task>

</tasks>

<verification>
1. Navigate to `/browse` — carousel auto-scrolls through category cards at top
2. Hovering over carousel pauses auto-scroll
3. Category tab row shows below carousel with "All" active by default
4. Clicking "Sports" tab updates URL to `?category=sports` and filters grid to sports properties only
5. Clicking "All" tab removes category filter
6. Clicking a carousel card also triggers the same category filter
7. Grid shows 5 columns on desktop, 3 on tablet, 2 on mobile
</verification>

<success_criteria>
- Auto-scrolling carousel with category image cards runs at top of browse page
- Carousel pauses on hover, resumes after mouse leaves
- Text-only category tab row appears below carousel
- Tab selection filters the card grid via URL searchParams
- BrowseClient manages filter state through router.replace
- No dot indicators or excessive navigation elements on carousel (clean feel)
</success_criteria>

<output>
After completion, create `.planning/phases/04-browse-and-discovery/04-02-SUMMARY.md`
</output>
