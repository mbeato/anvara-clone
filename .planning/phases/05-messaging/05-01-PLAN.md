---
phase: 05-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/messages/page.tsx
  - app/(app)/messages/loading.tsx
  - app/actions/message.ts
  - lib/data/index.ts
  - components/messaging/messaging-client.tsx
  - components/messaging/thread-list.tsx
  - components/messaging/thread-list-item.tsx

autonomous: true

user_setup:
  - service: openai
    why: "AI-simulated messaging responses via o3-mini"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys -> Create new secret key"

must_haves:
  truths:
    - "Messaging inbox displays a list of pre-seeded conversation threads — not empty"
    - "Thread list shows property image thumbnail, property name, last message snippet, timestamp"
    - "Threads are sorted by most recent message first"
    - "Clicking a thread updates the URL and selects it in the UI"
  artifacts:
    - path: "app/(app)/messages/page.tsx"
      provides: "RSC page fetching threads and rendering MessagingClient"
      min_lines: 10
    - path: "components/messaging/messaging-client.tsx"
      provides: "Two-panel client orchestrator with thread selection"
      min_lines: 30
    - path: "components/messaging/thread-list.tsx"
      provides: "Left panel thread list"
      min_lines: 15
    - path: "components/messaging/thread-list-item.tsx"
      provides: "Individual thread preview row with thumbnail, name, snippet, timestamp"
      min_lines: 25
    - path: "app/actions/message.ts"
      provides: "sendMessage server action"
      exports: ["sendMessage"]
    - path: "lib/data/index.ts"
      provides: "Updated getThread/getThreads with property.packages included"
      contains: "packages: true"
  key_links:
    - from: "app/(app)/messages/page.tsx"
      to: "lib/data/index.ts"
      via: "getThreads() import"
      pattern: "import.*getThreads.*from.*lib/data"
    - from: "app/(app)/messages/page.tsx"
      to: "components/messaging/messaging-client.tsx"
      via: "renders MessagingClient with threads prop"
      pattern: "<MessagingClient"
    - from: "components/messaging/messaging-client.tsx"
      to: "components/messaging/thread-list.tsx"
      via: "renders ThreadList"
      pattern: "<ThreadList"
---

<objective>
Install AI SDK dependencies, create the messaging page route with two-panel layout shell, build the thread list UI with pre-seeded data, and add the sendMessage server action.

Purpose: Establishes the messaging inbox foundation — the thread list panel and routing infrastructure that all subsequent messaging plans build on.
Output: Working /messages page showing pre-seeded thread list with property thumbnails, names, last message snippets, and timestamps. Thread selection updates URL. sendMessage action ready for use.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-messaging/05-CONTEXT.md
@.planning/phases/05-messaging/05-RESEARCH.md

# Key existing files
@lib/data/index.ts
@app/actions/offer.ts
@app/(app)/browse/page.tsx
@app/(app)/browse/_components/browse-client.tsx
@components/app-sidebar.tsx
@app/(app)/layout.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AI SDK, create sendMessage action, update data queries</name>
  <files>
    app/actions/message.ts
    lib/data/index.ts
    package.json
  </files>
  <action>
1. Install Vercel AI SDK dependencies:
   ```bash
   npm install ai @ai-sdk/openai
   ```
   Verify installation: `npm ls ai @ai-sdk/openai` should show versions.

2. Create `app/actions/message.ts` — sendMessage server action:
   - "use server" directive
   - Import prisma from "@/lib/prisma"
   - Import revalidatePath from "next/cache"
   - `sendMessage(threadId: string, content: string)` → creates Message with sender="advertiser", isAI=false
   - Returns `{ success: true, messageId: string }` or `{ success: false, error: string }`
   - Calls `revalidatePath("/messages")` on success
   - Wrap in try/catch, log errors with `[sendMessage]` prefix

3. Update `lib/data/index.ts` — add `packages: true` to property includes in both getThreads and getThread:
   - Change `property: true` to `property: { include: { packages: true } }` in BOTH functions
   - This is needed for the AI system prompt (packages/tiers) and the inline offer form (tier selection)
   - Do NOT change any other queries — only getThreads and getThread

4. Also update getThreads to order by most recent MESSAGE (not thread.createdAt) per CONTEXT.md locked decision:
   - Keep the current `orderBy: { createdAt: "desc" }` on findMany (good enough since seed data creates threads in message order)
   - The client-side sorting will handle reordering when new messages arrive
  </action>
  <verify>
    - `npm ls ai @ai-sdk/openai` shows both packages installed
    - `npx tsc --noEmit` passes without type errors
    - `app/actions/message.ts` exists with "use server" directive and sendMessage export
    - `lib/data/index.ts` getThread and getThreads both include `property: { include: { packages: true } }`
  </verify>
  <done>
    AI SDK installed. sendMessage server action saves advertiser messages to DB. getThread/getThreads return property with packages for AI prompt and offer form.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create messaging page route, MessagingClient shell, thread list components</name>
  <files>
    app/(app)/messages/page.tsx
    app/(app)/messages/loading.tsx
    components/messaging/messaging-client.tsx
    components/messaging/thread-list.tsx
    components/messaging/thread-list-item.tsx
  </files>
  <action>
1. Create `app/(app)/messages/page.tsx` — React Server Component:
   - Import getThreads from "@/lib/data"
   - Import MessagingClient from "@/components/messaging/messaging-client"
   - Async function that awaits searchParams (Next.js 16 pattern: `searchParams: Promise<{ thread?: string }>`)
   - Call `getThreads()` to fetch all threads with messages, offers, property+packages
   - Render `<MessagingClient threads={threads} initialThreadId={params.thread} />`
   - Wrap MessagingClient in `<Suspense>` as safety for useSearchParams

2. Create `app/(app)/messages/loading.tsx` — skeleton:
   - Two-panel skeleton matching the inbox layout
   - Left panel: 5 skeleton rows (thumbnail circle + 2 text lines each)
   - Right panel: centered "Select a conversation" placeholder text
   - Use Skeleton from @/components/ui/skeleton
   - Full height: `h-[calc(100vh-4rem)]` matching the main area

3. Create `components/messaging/messaging-client.tsx` — "use client":
   - Props: threads array (typed from Prisma return type), initialThreadId?: string
   - Use useSearchParams + useRouter from next/navigation for thread selection
   - Active thread: searchParams.get("thread") ?? initialThreadId ?? threads[0]?.id
   - selectThread function: router.push(`/messages?thread=${threadId}`)
   - Two-panel layout: `flex h-[calc(100vh-4rem)] overflow-hidden`
   - Left panel: `w-80 border-r flex flex-col overflow-hidden` → renders ThreadList
   - Right panel: `flex-1 flex flex-col overflow-hidden` → placeholder text "Select a conversation" (ConversationView comes in 05-02)
   - Find activeThread from threads array by activeThreadId for highlighting

4. Create `components/messaging/thread-list.tsx`:
   - Props: threads array, activeThreadId: string | null, onSelectThread callback
   - Header: "Messages" title with count badge (e.g., "5")
   - Scrollable list: `flex-1 overflow-y-auto`
   - Maps threads to ThreadListItem components

5. Create `components/messaging/thread-list-item.tsx`:
   - Props: thread data, isActive boolean, onClick callback
   - Layout: horizontal flex with property image thumbnail (40x40 rounded-full, use next/Image with thread.property.imageUrl), property name (bold if unread — for now, bold the first thread as demo), last message snippet (truncated to ~50 chars), relative timestamp
   - Offer status badge: if thread.offers.length > 0, show Badge with latest offer status (Pending/Accepted/Declined)
   - Active state: bg-accent/50 background when isActive
   - Unread indicator: colored dot (w-2 h-2 rounded-full bg-primary) — show on first 2 threads as demo unread state
   - Timestamp: use native `toLocaleDateString` or relative like "Today", "Yesterday", "Feb 19"
   - Truncate last message: `line-clamp-1` or substring
  </action>
  <verify>
    - `npm run dev` starts without errors
    - Navigate to http://localhost:3000/messages — page loads showing thread list
    - Thread list shows 5 pre-seeded threads with property names, thumbnails, last message snippets
    - Clicking a thread updates the URL to /messages?thread={id}
    - Active thread has highlighted background
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    /messages page renders a two-panel inbox. Left panel shows pre-seeded thread list with property thumbnails, names, message snippets, timestamps, and offer badges. Thread selection updates URL. Right panel shows placeholder (conversation view added in 05-02).
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` — no errors
2. `npx tsc --noEmit` — no type errors
3. Navigate to /messages — thread list displays with 5 pre-seeded threads
4. Each thread shows: property image, property name, last message snippet, timestamp
5. Clicking a thread changes URL to /messages?thread={id} and highlights it
6. Sidebar "Messages" link navigates to /messages correctly
7. `npm ls ai @ai-sdk/openai` confirms AI SDK installed
</verification>

<success_criteria>
- Pre-seeded thread list visible on /messages (not empty inbox)
- Thread previews show property thumbnail, name, snippet, timestamp, offer badge
- Thread selection via click updates URL and highlights active thread
- sendMessage server action exists and is importable
- AI SDK packages installed for use in 05-02
</success_criteria>

<output>
After completion, create `.planning/phases/05-messaging/05-01-SUMMARY.md`
</output>
