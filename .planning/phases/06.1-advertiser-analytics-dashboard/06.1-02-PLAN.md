---
phase: 06.1-advertiser-analytics-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - app/(app)/dashboard/_components/impressions-area-chart.tsx
  - app/(app)/dashboard/_components/category-bar-chart.tsx
  - app/(app)/dashboard/_components/recent-activity-table.tsx
  - app/(app)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard page displays 4 KPI cards in a responsive grid showing impressions, campaigns, conversion rate, and deal pipeline"
    - "Area chart renders a 30-day impressions trend with gradient fill and tooltip"
    - "Bar chart renders category breakdown showing property counts or reach per category"
    - "Recent activity table shows latest offers and messages with property names and dates"
    - "All data is derived from seeded properties — no hardcoded fake numbers in components"
    - "Dashboard page is accessible at /dashboard and renders inside the sidebar layout"
  artifacts:
    - path: "app/(app)/dashboard/_components/impressions-area-chart.tsx"
      provides: "30-day impressions area chart with gradient"
      exports: ["ImpressionsAreaChart"]
      contains: "use client"
    - path: "app/(app)/dashboard/_components/category-bar-chart.tsx"
      provides: "Category breakdown bar chart"
      exports: ["CategoryBarChart"]
      contains: "use client"
    - path: "app/(app)/dashboard/_components/recent-activity-table.tsx"
      provides: "Recent activity table with offers and messages"
      exports: ["RecentActivityTable"]
    - path: "app/(app)/dashboard/page.tsx"
      provides: "Dashboard page Server Component assembling all sections"
      contains: "async function"
  key_links:
    - from: "app/(app)/dashboard/page.tsx"
      to: "lib/data/index.ts"
      via: "getAllProperties() and getThreads() calls"
      pattern: "await.*getAllProperties|await.*getThreads"
    - from: "app/(app)/dashboard/page.tsx"
      to: "lib/data/analytics.ts"
      via: "deriveAnalytics() call"
      pattern: "deriveAnalytics"
    - from: "app/(app)/dashboard/page.tsx"
      to: "app/(app)/dashboard/_components/*"
      via: "component imports and prop passing"
      pattern: "import.*from.*\\./_components/"
    - from: "app/(app)/dashboard/_components/impressions-area-chart.tsx"
      to: "components/ui/chart.tsx"
      via: "ChartContainer and ChartTooltip usage"
      pattern: "ChartContainer|ChartTooltip"
---

<objective>
Build the chart components, recent activity table, and assemble the full dashboard page that wires everything together.

Purpose: This is the final assembly plan — after this, the dashboard is fully functional at `/dashboard` with KPI cards, an area chart, a bar chart, and a recent activity table, all powered by derived analytics from seeded data.

Output: Three chart/table components and the `page.tsx` Server Component that fetches data, derives analytics, and renders the complete dashboard.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-advertiser-analytics-dashboard/06.1-RESEARCH.md
@.planning/phases/06.1-advertiser-analytics-dashboard/06.1-01-SUMMARY.md

Reference files:
@lib/data/analytics.ts — analytics derivation helpers (created in Plan 01)
@lib/data/index.ts — getAllProperties() and getThreads()
@app/(app)/dashboard/_components/kpi-card.tsx — KPI card component (created in Plan 01)
@components/ui/chart.tsx — ChartContainer, ChartTooltip, ChartTooltipContent, ChartConfig
@components/property-detail/property-demographics.tsx — existing ChartContainer + BarChart pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create impressions area chart and category bar chart</name>
  <files>app/(app)/dashboard/_components/impressions-area-chart.tsx, app/(app)/dashboard/_components/category-bar-chart.tsx</files>
  <action>
**Impressions Area Chart (`impressions-area-chart.tsx`):**
- `"use client"` as first line (required — Recharts needs client)
- Import `AreaChart`, `Area`, `XAxis`, `YAxis`, `CartesianGrid` from `recharts`
- Import `ChartContainer`, `ChartTooltip`, `ChartTooltipContent` from `@/components/ui/chart`
- Import `type { ChartConfig }` from `@/components/ui/chart` (use `import type`)
- Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`

- Chart config:
  ```typescript
  const impressionsConfig = {
    impressions: {
      label: "Impressions",
      color: "var(--color-chart-1)",
    },
  } satisfies ChartConfig
  ```

- Props: `{ data: Array<{ date: string; impressions: number }> }`
- Wrap in `Card` with heading "Impressions Trend" in `CardHeader`
- `ChartContainer` with `config={impressionsConfig}` and `className="h-64 w-full"`
- Inside: `AreaChart` with `data={data}` and `margin={{ top: 4, right: 4, bottom: 0, left: 0 }}`
  - `<defs>` with `linearGradient` id="impressionsGradient" going from 30% opacity to 0%
  - `CartesianGrid strokeDasharray="3 3" vertical={false}`
  - `XAxis dataKey="date"` with `tickLine={false} axisLine={false} tick={{ fontSize: 11 }}`
  - `YAxis` with `tickLine={false} axisLine={false} tick={{ fontSize: 11 }}` and `tickFormatter` that formats thousands as "K" (e.g., 42000 -> "42K")
  - `ChartTooltip content={<ChartTooltipContent />}`
  - `Area type="monotone" dataKey="impressions" stroke="var(--color-impressions)" fill="url(#impressionsGradient)"`

**Category Bar Chart (`category-bar-chart.tsx`):**
- `"use client"` as first line
- Import `BarChart`, `Bar`, `XAxis`, `YAxis`, `CartesianGrid` from `recharts`
- Import `ChartContainer`, `ChartTooltip`, `ChartTooltipContent` from `@/components/ui/chart`
- Import `type { ChartConfig }` from `@/components/ui/chart`
- Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`

- Chart config:
  ```typescript
  const categoryConfig = {
    properties: {
      label: "Properties",
      color: "var(--color-chart-2)",
    },
  } satisfies ChartConfig
  ```

- Props: `{ data: Array<{ category: string; properties: number; totalReach: number; avgPrice: number }> }`
- Wrap in `Card` with heading "Properties by Category" in `CardHeader`
- `ChartContainer` with `config={categoryConfig}` and `className="h-64 w-full"`
- Inside: `BarChart` with `data={data}` and `margin={{ top: 4, right: 4, bottom: 0, left: 0 }}`
  - `CartesianGrid strokeDasharray="3 3" vertical={false}`
  - `XAxis dataKey="category"` with `tickLine={false} axisLine={false} tick={{ fontSize: 11 }}`
  - `YAxis` with `tickLine={false} axisLine={false} tick={{ fontSize: 11 }}`
  - `ChartTooltip content={<ChartTooltipContent />}`
  - `Bar dataKey="properties" fill="var(--color-properties)" radius={[4, 4, 0, 0]}`
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify both files have `"use client"` as first line. Verify both use `ChartContainer` (not raw `ResponsiveContainer`). Verify both use `import type` for `ChartConfig`.
  </verify>
  <done>
Impressions area chart renders a 30-day gradient-filled trend with tooltip. Category bar chart renders 5 category bars with rounded tops and tooltip. Both use ChartContainer for theming and responsive sizing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create recent activity table and assemble dashboard page</name>
  <files>app/(app)/dashboard/_components/recent-activity-table.tsx, app/(app)/dashboard/page.tsx</files>
  <action>
**Recent Activity Table (`recent-activity-table.tsx`):**
- Server Component (NO `'use client'` — pure HTML table, no Recharts)
- Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`
- Import `Badge` from `@/components/ui/badge`

- Props: `{ data: Array<{ propertyName: string; type: "offer" | "message"; detail: string; date: string }> }`
- Wrap in `Card` with heading "Recent Activity" in `CardHeader`
- Render `<table>` with Tailwind classes: `w-full text-sm`
  - `<thead>`: columns — Property, Type, Detail, Date
  - `<tbody>`: map over data, each row with:
    - Property name (font-medium)
    - Type as `Badge` — variant="outline" for message, variant="default" for offer
    - Detail (text-muted-foreground, truncate max-w-xs)
    - Date (text-muted-foreground, text-right)
  - Table styling: `divide-y` on tbody, `border-b` on thead, `px-4 py-3` on cells
  - If data is empty, show "No recent activity" message

**Dashboard Page (`app/(app)/dashboard/page.tsx`):**
- Server Component (NO `'use client'` — this is an async page)
- Import `getAllProperties`, `getThreads` from `@/lib/data`
- Import `deriveAnalytics` from `@/lib/data/analytics`
- Import `KpiCard` from `./_components/kpi-card`
- Import `ImpressionsAreaChart` from `./_components/impressions-area-chart`
- Import `CategoryBarChart` from `./_components/category-bar-chart`
- Import `RecentActivityTable` from `./_components/recent-activity-table`
- Import icon components for KPI cards: `ChartArea`, `TrendingUp`, `Briefcase`, `MessageSquare` from `lucide-react`

- `export default async function DashboardPage()`:
  1. Fetch data: `const [properties, threads] = await Promise.all([getAllProperties(), getThreads()])`
  2. Derive analytics: `const analytics = deriveAnalytics(properties, threads)`
  3. Map `iconName` strings from analytics.kpis to actual icon components using a lookup object:
     ```typescript
     const iconMap = { ChartArea, TrendingUp, Briefcase, MessageSquare }
     ```

  4. Render layout:
     ```
     <div className="space-y-6 p-6 max-w-7xl mx-auto">
       {/* Page heading */}
       <div>
         <h1 className="text-2xl font-bold tracking-tight">Campaign Analytics</h1>
         <p className="text-muted-foreground">Performance overview across your sponsorship portfolio</p>
       </div>

       {/* KPI row: 2 cols on mobile, 4 cols on lg */}
       <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">
         {analytics.kpis.map((kpi) => (
           <KpiCard key={kpi.label} label={kpi.label} value={kpi.value} trend={kpi.trend} icon={iconMap[kpi.iconName]} />
         ))}
       </div>

       {/* Charts row: full width stacked on mobile, 2/3 + 1/3 on lg */}
       <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
         <div className="lg:col-span-2">
           <ImpressionsAreaChart data={analytics.impressionsTrend} />
         </div>
         <CategoryBarChart data={analytics.categoryBreakdown} />
       </div>

       {/* Recent activity table */}
       <RecentActivityTable data={analytics.recentActivity} />
     </div>
     ```

- Add Next.js metadata export: `export const metadata = { title: "Campaign Analytics | Anvara" }`
  </action>
  <verify>
1. Run `npx tsc --noEmit` — no TypeScript errors
2. Run `npm run build` — build completes without errors
3. Verify `page.tsx` does NOT have `'use client'`
4. Verify `page.tsx` imports from `@/lib/data` and `@/lib/data/analytics`
5. Verify `recent-activity-table.tsx` does NOT have `'use client'`
  </verify>
  <done>
Dashboard page at `/dashboard` renders complete: 4 KPI cards with trend indicators, impressions area chart with gradient fill, category bar chart, and recent activity table. All data derived from seeded properties via analytics helper. Page renders inside the sidebar layout shell. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with no errors
2. Navigate to `/dashboard` — page loads with KPI cards, area chart, bar chart, and activity table
3. KPI cards show non-zero values with trend indicators (up arrows, green text)
4. Area chart shows 30 data points with gradient fill and working tooltip on hover
5. Bar chart shows 5 category bars (Sports, Lifestyle, Music, Food, Arts)
6. Recent activity table shows property names, offer/message badges, and dates
7. Sidebar highlights Dashboard with ChartArea icon when on /dashboard
8. Loading skeleton appears briefly before data loads
9. No `'use client'` on page.tsx or recent-activity-table.tsx
10. Both chart components have `'use client'` as first line
</verification>

<success_criteria>
- Dashboard page fully assembled at `/dashboard` with all 4 sections (KPIs, area chart, bar chart, activity table)
- All data derives from seeded properties — changing seed data would change dashboard numbers
- Area chart shows 30-day trend with gradient fill and tooltip
- Bar chart shows category breakdown with 5 bars
- Recent activity table shows up to 8 entries with type badges
- Build passes cleanly with no TypeScript errors
- Page renders inside the existing sidebar layout without any layout.tsx changes
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-advertiser-analytics-dashboard/06.1-02-SUMMARY.md`
</output>
