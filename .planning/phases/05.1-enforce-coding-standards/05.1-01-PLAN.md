---
phase: 05.1-enforce-coding-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/browse/_components/browse-client.tsx
  - app/_components/landing/az-execution.tsx
  - app/_components/landing/for-brands-rightsholders.tsx
  - app/_components/landing/faq-section.tsx
  - app/(app)/browse/_components/recommendations-strip.tsx
  - app/(app)/messages/page.tsx
autonomous: true

must_haves:
  truths:
    - "No unnecessary useEffect hooks remain — only legitimate browser API / animation effects"
    - "'use client' only on components that use hooks, state, or browser APIs"
    - "No dead imports (every imported symbol is used)"
    - "No redundant Suspense boundaries wrapping already-resolved data"
    - "Build passes cleanly with no TypeScript errors"
  artifacts:
    - path: "app/(app)/browse/_components/browse-client.tsx"
      provides: "Browse client without redundant skeleton useEffect"
      contains: "properties.length === 0"
    - path: "app/_components/landing/az-execution.tsx"
      provides: "Clean import without unused useEffect"
    - path: "app/_components/landing/for-brands-rightsholders.tsx"
      provides: "Server Component without use client directive"
    - path: "app/_components/landing/faq-section.tsx"
      provides: "Server Component without use client directive"
    - path: "app/(app)/browse/_components/recommendations-strip.tsx"
      provides: "Server Component without use client directive"
    - path: "app/(app)/messages/page.tsx"
      provides: "Messages page without redundant Suspense"
  key_links:
    - from: "app/(app)/browse/_components/browse-client.tsx"
      to: "app/(app)/browse/loading.tsx"
      via: "loading.tsx handles route-level skeleton; no client skeleton needed"
      pattern: "loading\\.tsx"
---

<objective>
Remove unnecessary `useEffect` hooks, dead imports, redundant `"use client"` directives, and bare `<Suspense>` wrappers across the codebase.

Purpose: Enforce React 19 / Next.js Server Component best practices — `"use client"` only on interactive leaf components, no unused hooks, no redundant loading mechanisms.
Output: Six files cleaned up, build still passes.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-enforce-coding-standards/05.1-RESEARCH.md
@app/(app)/browse/_components/browse-client.tsx
@app/_components/landing/az-execution.tsx
@app/_components/landing/for-brands-rightsholders.tsx
@app/_components/landing/faq-section.tsx
@app/(app)/browse/_components/recommendations-strip.tsx
@app/(app)/messages/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant useEffect skeleton + refactor removeFilter in browse-client.tsx</name>
  <files>app/(app)/browse/_components/browse-client.tsx</files>
  <action>
  In `browse-client.tsx`, make these changes:

  1. **Remove the `isLoading` state and `useEffect` skeleton delay** (lines ~43-47):
     - Delete `const [isLoading, setIsLoading] = useState(true);`
     - Delete the entire `useEffect(() => { const timer = setTimeout(... }, []);` block
     - Remove `useState` from the React import if no other `useState` usage remains (check: `useMemo` and `useCallback` are still used). NOTE: `useState` is NOT used elsewhere — remove it from the import.
     - Remove `useEffect` from the React import.

  2. **Remove the skeleton render branch** (~lines 138-144):
     - Delete the `{isLoading ? ( <div ...skeletons... </div> ) :` ternary wrapper
     - Keep the `properties.length === 0 ? <EmptyState /> : <grid>` logic as the direct render

  3. **Remove `PropertyCardSkeleton` import** (line ~9):
     - Delete `import { PropertyCardSkeleton } from "./property-card-skeleton";`
     - The skeleton is only used in the now-removed loading branch. `loading.tsx` has its own skeleton imports.

  4. **Refactor `removeFilter` to eliminate eslint-disable** (~lines 89-95):
     - Replace the current `removeFilter` + eslint-disable pattern with a clean `useCallback` that builds URL params directly (same pattern as `handlePriceChange`):
     ```tsx
     const removeFilter = useCallback(
       (key: string) => {
         const params = new URLSearchParams();
         if (key !== "category" && currentFilters.category) params.set("category", currentFilters.category);
         if (key !== "region" && currentFilters.region) params.set("region", currentFilters.region);
         if (key !== "minPrice" && currentFilters.minPrice !== undefined) params.set("minPrice", String(currentFilters.minPrice));
         if (key !== "maxPrice" && currentFilters.maxPrice !== undefined) params.set("maxPrice", String(currentFilters.maxPrice));
         const qs = params.toString();
         router.replace(pathname + (qs ? "?" + qs : ""), { scroll: false });
       },
       [currentFilters, pathname, router]
     );
     ```
     - Remove the `// eslint-disable-next-line react-hooks/exhaustive-deps` comment entirely.

  After these changes, the React import should be: `import { useCallback, useMemo } from "react";`
  </action>
  <verify>
  Run `npx tsc --noEmit` — zero errors. Then `npm run build` — clean build. Grep for `isLoading` in browse-client.tsx — no matches. Grep for `eslint-disable` in browse-client.tsx — no matches.
  </verify>
  <done>
  browse-client.tsx has no useEffect, no useState, no isLoading skeleton branch, no eslint-disable suppression. The component renders properties directly (grid or empty state). The route-level loading.tsx handles navigation skeletons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unnecessary "use client" directives + dead imports + bare Suspense</name>
  <files>
    app/_components/landing/az-execution.tsx
    app/_components/landing/for-brands-rightsholders.tsx
    app/_components/landing/faq-section.tsx
    app/(app)/browse/_components/recommendations-strip.tsx
    app/(app)/messages/page.tsx
  </files>
  <action>
  Make these five targeted edits:

  1. **`az-execution.tsx`** — Remove `useEffect` from the import line. Change:
     `import { useState, useRef, useEffect, useCallback } from "react";`
     to:
     `import { useState, useRef, useCallback } from "react";`
     (useEffect is imported but never called in this component)

  2. **`for-brands-rightsholders.tsx`** — Delete the `"use client";` directive on line 1. This component has no hooks, no state, no browser APIs. Its children (`SectionReveal`, `Link`) carry their own client directives.

  3. **`faq-section.tsx`** — Delete the `"use client";` directive on line 1. The ShadCN `Accordion` component already has `"use client"` internally. This parent only renders static JSX + Accordion children.

  4. **`recommendations-strip.tsx`** — Delete the `"use client"` directive on line 1. This component has no hooks, no state, no browser APIs — it just maps over properties and renders `PropertyCard` components.

  5. **`messages/page.tsx`** — Remove the `<Suspense>` wrapper around `<MessagingClient>`. The data is already fetched (`await getThreads()`) before render, so Suspense serves no purpose here. Also remove the `import { Suspense } from "react"` line since it's no longer used. The page should directly render:
     ```tsx
     return (
       <MessagingClient threads={threads} initialThreadId={params.thread} />
     )
     ```
  </action>
  <verify>
  Run `npx tsc --noEmit` — zero errors. Run `npm run build` — clean build. Verify:
  - `grep -r "useEffect" app/_components/landing/az-execution.tsx` returns only zero matches for the import line (the word may appear in comments)
  - `grep "use client" app/_components/landing/for-brands-rightsholders.tsx` returns nothing
  - `grep "use client" app/_components/landing/faq-section.tsx` returns nothing
  - `grep "use client" app/(app)/browse/_components/recommendations-strip.tsx` returns nothing
  - `grep "Suspense" app/(app)/messages/page.tsx` returns nothing
  </verify>
  <done>
  Five files cleaned: az-execution.tsx has no dead useEffect import; for-brands-rightsholders.tsx, faq-section.tsx, and recommendations-strip.tsx are Server Components; messages/page.tsx has no redundant Suspense wrapper.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — clean build with no errors
3. No `useEffect` in browse-client.tsx
4. No `eslint-disable` in browse-client.tsx
5. No `"use client"` in for-brands-rightsholders.tsx, faq-section.tsx, recommendations-strip.tsx
6. No `Suspense` in messages/page.tsx
7. No dead `useEffect` import in az-execution.tsx
</verification>

<success_criteria>
All seven violations from the RESEARCH.md HIGH and MEDIUM priority lists are fixed. The LOW priority eslint-disable is also fixed. Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-enforce-coding-standards/05.1-01-SUMMARY.md`
</output>
