---
phase: 05.1-enforce-coding-standards
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - tsconfig.json
autonomous: true

must_haves:
  truths:
    - "TypeScript compiler enforces import type for type-only imports via verbatimModuleSyntax"
    - "All imports use import type where appropriate — no compiler errors"
    - "Build passes cleanly with the stricter tsconfig setting"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript config with verbatimModuleSyntax enabled"
      contains: "verbatimModuleSyntax"
  key_links:
    - from: "tsconfig.json"
      to: "all .ts/.tsx files"
      via: "compiler enforces import type at build time"
      pattern: "verbatimModuleSyntax.*true"
---

<objective>
Enable `verbatimModuleSyntax: true` in `tsconfig.json` to enforce `import type` at the TypeScript compiler level, then fix any resulting import errors.

Purpose: Make `import type` enforcement a compile-time guarantee rather than a convention. Any future type-only imports that forget `import type` will be caught by `tsc`.
Output: Updated tsconfig.json + any import fixes needed for compliance.
</objective>

<execution_context>
@/Users/vtx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vtx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-enforce-coding-standards/05.1-RESEARCH.md
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable verbatimModuleSyntax and fix resulting import errors</name>
  <files>tsconfig.json</files>
  <action>
  1. In `tsconfig.json`, add `"verbatimModuleSyntax": true` to `compilerOptions`.

  2. Remove `"isolatedModules": true` — it is implied by `verbatimModuleSyntax` and keeping both may cause a TypeScript warning.

  3. Run `npx tsc --noEmit` to check for errors.

  4. **If errors appear** (likely pattern: `import { SomeType }` needs to be `import type { SomeType }`):
     - For each error, change `import { X }` to `import type { X }` where X is only used as a type
     - For mixed imports (both values and types from same module), split into separate import statements:
       ```tsx
       // Before
       import { SomeComponent, SomeType } from "./module";
       // After
       import { SomeComponent } from "./module";
       import type { SomeType } from "./module";
       ```
     - Pay special attention to `lib/data/index.ts` re-exports — they may need `export type` syntax
     - Do NOT modify files in `prisma/generated/` or `components/ui/` (ShadCN). If errors come from these, add them to tsconfig `exclude` array.

  5. Run `npx tsc --noEmit` again — must be zero errors.

  6. Run `npm run build` — must pass cleanly.

  **Known patterns already correct (should not error):**
  - `import type { getAllProperties }` in browse components — already correct
  - `import type { getThreads }` in messaging components — already correct
  - `import type { Metadata }` in layout.tsx — already correct
  - `import type { ChartConfig }` in demographics — already correct
  </action>
  <verify>
  Run `npx tsc --noEmit` — zero errors. Run `npm run build` — clean build. Verify `verbatimModuleSyntax` is in tsconfig: `grep "verbatimModuleSyntax" tsconfig.json` returns a match.
  </verify>
  <done>
  tsconfig.json enforces `import type` at compiler level. All existing imports comply. Future type-only imports that forget `import type` will produce a TypeScript error.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors with `verbatimModuleSyntax: true`
2. `npm run build` — clean build
3. `tsconfig.json` contains `"verbatimModuleSyntax": true`
4. `tsconfig.json` does NOT contain `"isolatedModules": true` (redundant)
</verification>

<success_criteria>
TypeScript compiler now enforces `import type` for type-only imports. Build passes. All existing imports comply with the stricter setting.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-enforce-coding-standards/05.1-02-SUMMARY.md`
</output>
